<!-- Generated by mini-go doc. Target: minigo. -->

# Package `time`

```go
package time
```

Package time provides durations, Gregorian calendar values, formatting, parsing, clocks, and timers.

## Index

- [const ANSIC](#const-ansic)
- [func After](#func-after)
- [func AfterFunc](#func-afterfunc)
- [const April](#const-april)
- [const August](#const-august)
- [func Date](#func-date)
- [const DateOnly](#const-dateonly)
- [const DateTime](#const-datetime)
- [const December](#const-december)
- [type Duration](#type-duration)
  - [Duration.Abs](#func-durationabs)
  - [Duration.Hours](#func-durationhours)
  - [Duration.Microseconds](#func-durationmicroseconds)
  - [Duration.Milliseconds](#func-durationmilliseconds)
  - [Duration.Minutes](#func-durationminutes)
  - [Duration.Nanoseconds](#func-durationnanoseconds)
  - [Duration.Round](#func-durationround)
  - [Duration.Seconds](#func-durationseconds)
  - [Duration.String](#func-durationstring)
  - [Duration.Truncate](#func-durationtruncate)
- [const February](#const-february)
- [func FixedZone](#func-fixedzone)
- [const Friday](#const-friday)
- [const Hour](#const-hour)
- [const January](#const-january)
- [const July](#const-july)
- [const June](#const-june)
- [const Kitchen](#const-kitchen)
- [const Layout](#const-layout)
- [func LoadLocation](#func-loadlocation)
- [func LoadLocationFromTZData](#func-loadlocationfromtzdata)
- [var Local](#var-local)
- [type Location](#type-location)
  - [Location.String](#func-locationstring)
- [const March](#const-march)
- [const May](#const-may)
- [const Microsecond](#const-microsecond)
- [const Millisecond](#const-millisecond)
- [const Minute](#const-minute)
- [const Monday](#const-monday)
- [type Month](#type-month)
  - [Month.String](#func-monthstring)
- [const Nanosecond](#const-nanosecond)
- [func NewTicker](#func-newticker)
- [func NewTimer](#func-newtimer)
- [const November](#const-november)
- [func Now](#func-now)
- [const October](#const-october)
- [func Parse](#func-parse)
- [func ParseDuration](#func-parseduration)
- [type ParseError](#type-parseerror)
  - [ParseError.Error](#func-parseerrorerror)
- [func ParseInLocation](#func-parseinlocation)
- [const RFC1123](#const-rfc1123)
- [const RFC1123Z](#const-rfc1123z)
- [const RFC3339](#const-rfc3339)
- [const RFC3339Nano](#const-rfc3339nano)
- [const RFC822](#const-rfc822)
- [const RFC822Z](#const-rfc822z)
- [const RFC850](#const-rfc850)
- [const RubyDate](#const-rubydate)
- [const Saturday](#const-saturday)
- [const Second](#const-second)
- [const September](#const-september)
- [func Since](#func-since)
- [func Sleep](#func-sleep)
- [const Stamp](#const-stamp)
- [const StampMicro](#const-stampmicro)
- [const StampMilli](#const-stampmilli)
- [const StampNano](#const-stampnano)
- [const Sunday](#const-sunday)
- [const Thursday](#const-thursday)
- [func Tick](#func-tick)
- [type Ticker](#type-ticker)
  - [Ticker.Reset](#func-tickerreset)
  - [Ticker.Stop](#func-tickerstop)
- [type Time](#type-time)
  - [Time.Add](#func-timeadd)
  - [Time.AddDate](#func-timeadddate)
  - [Time.After](#func-timeafter)
  - [Time.AppendBinary](#func-timeappendbinary)
  - [Time.AppendFormat](#func-timeappendformat)
  - [Time.AppendText](#func-timeappendtext)
  - [Time.Before](#func-timebefore)
  - [Time.Clock](#func-timeclock)
  - [Time.Compare](#func-timecompare)
  - [Time.Date](#func-timedate)
  - [Time.Day](#func-timeday)
  - [Time.Equal](#func-timeequal)
  - [Time.Format](#func-timeformat)
  - [Time.GoString](#func-timegostring)
  - [Time.GobDecode](#func-timegobdecode)
  - [Time.GobEncode](#func-timegobencode)
  - [Time.Hour](#func-timehour)
  - [Time.ISOWeek](#func-timeisoweek)
  - [Time.In](#func-timein)
  - [Time.IsDST](#func-timeisdst)
  - [Time.IsZero](#func-timeiszero)
  - [Time.Local](#func-timelocal)
  - [Time.Location](#func-timelocation)
  - [Time.MarshalBinary](#func-timemarshalbinary)
  - [Time.MarshalJSON](#func-timemarshaljson)
  - [Time.MarshalText](#func-timemarshaltext)
  - [Time.Minute](#func-timeminute)
  - [Time.Month](#func-timemonth)
  - [Time.Nanosecond](#func-timenanosecond)
  - [Time.Round](#func-timeround)
  - [Time.Second](#func-timesecond)
  - [Time.String](#func-timestring)
  - [Time.Sub](#func-timesub)
  - [Time.Truncate](#func-timetruncate)
  - [Time.UTC](#func-timeutc)
  - [Time.Unix](#func-timeunix)
  - [Time.UnixMicro](#func-timeunixmicro)
  - [Time.UnixMilli](#func-timeunixmilli)
  - [Time.UnixNano](#func-timeunixnano)
  - [Time.UnmarshalBinary](#func-timeunmarshalbinary)
  - [Time.UnmarshalJSON](#func-timeunmarshaljson)
  - [Time.UnmarshalText](#func-timeunmarshaltext)
  - [Time.Weekday](#func-timeweekday)
  - [Time.Year](#func-timeyear)
  - [Time.YearDay](#func-timeyearday)
  - [Time.Zone](#func-timezone)
  - [Time.ZoneBounds](#func-timezonebounds)
- [const TimeOnly](#const-timeonly)
- [type Timer](#type-timer)
  - [Timer.Reset](#func-timerreset)
  - [Timer.Stop](#func-timerstop)
- [const Tuesday](#const-tuesday)
- [var UTC](#var-utc)
- [func Unix](#func-unix)
- [const UnixDate](#const-unixdate)
- [func UnixMicro](#func-unixmicro)
- [func UnixMilli](#func-unixmilli)
- [func Until](#func-until)
- [const Wednesday](#const-wednesday)
- [type Weekday](#type-weekday)
  - [Weekday.String](#func-weekdaystring)

## Constants

### const ANSIC

```go
const ANSIC = "Mon Jan _2 15:04:05 2006"
```

The reference time, in numerical order.

### const April

```go

```

April is the fourth month of the year.

### const August

```go

```

August is the eighth month of the year.

### const DateOnly

```go
const DateOnly = "2006-01-02"
```

These are predefined layouts for use in [Time.Format](time.md#func-timeformat) and [time.Parse](time.md#func-parse). The reference time used in these layouts is the specific time stamp:

	01/02 03:04:05PM '06 -0700

(January 2, 15:04:05, 2006, in time zone seven hours west of GMT). That value is recorded as the constant named [Layout](time.md#const-layout), listed below. As a Unix time, this is 1136239445. Since MST is GMT-0700, the reference would be printed by the Unix date command as:

	Mon Jan 2 15:04:05 MST 2006

It is a regrettable historic error that the date uses the American convention of putting the numerical month before the day.

The example for Time.Format demonstrates the working of the layout string in detail and is a good reference.

Note that the [RFC822](time.md#const-rfc822), [RFC850](time.md#const-rfc850), and [RFC1123](time.md#const-rfc1123) formats should be applied only to local times. Applying them to UTC times will use "UTC" as the time zone abbreviation, while strictly speaking those RFCs require the use of "GMT" in that case. When using the [RFC1123](time.md#const-rfc1123) or [RFC1123Z](time.md#const-rfc1123z) formats for parsing, note that these formats define a leading zero for the day-in-month portion, which is not strictly allowed by RFC 1123. This will result in an error when parsing date strings that occur in the first 9 days of a given month. In general [RFC1123Z](time.md#const-rfc1123z) should be used instead of [RFC1123](time.md#const-rfc1123) for servers that insist on that format, and [RFC3339](time.md#const-rfc3339) should be preferred for new protocols. [RFC3339](time.md#const-rfc3339), [RFC822](time.md#const-rfc822), [RFC822Z](time.md#const-rfc822z), [RFC1123](time.md#const-rfc1123), and [RFC1123Z](time.md#const-rfc1123z) are useful for formatting; when used with time.Parse they do not accept all the time formats permitted by the RFCs and they do accept time formats not formally defined. The [RFC3339Nano](time.md#const-rfc3339nano) format removes trailing zeros from the seconds field and thus may not sort correctly once formatted.

Most programs can use one of the defined constants as the layout passed to Format or Parse. The rest of this comment can be ignored unless you are creating a custom layout string.

To define your own format, write down what the reference time would look like formatted your way; see the values of constants like [ANSIC](time.md#const-ansic), [StampMicro](time.md#const-stampmicro) or [Kitchen](time.md#const-kitchen) for examples. The model is to demonstrate what the reference time looks like so that the Format and Parse methods can apply the same transformation to a general time value.

Here is a summary of the components of a layout string. Each element shows by example the formatting of an element of the reference time. Only these values are recognized. Text in the layout string that is not recognized as part of the reference time is echoed verbatim during Format and expected to appear verbatim in the input to Parse.

	Year: "2006" "06"
	Month: "Jan" "January" "01" "1"
	Day of the week: "Mon" "Monday"
	Day of the month: "2" "_2" "02"
	Day of the year: "__2" "002"
	Hour: "15" "3" "03" (PM or AM)
	Minute: "4" "04"
	Second: "5" "05"
	AM/PM mark: "PM"

Numeric time zone offsets format as follows:

	"-0700"     ±hhmm
	"-07:00"    ±hh:mm
	"-07"       ±hh
	"-070000"   ±hhmmss
	"-07:00:00" ±hh:mm:ss

Replacing the sign in the format with a Z triggers the ISO 8601 behavior of printing Z instead of an offset for the UTC zone. Thus:

	"Z0700"      Z or ±hhmm
	"Z07:00"     Z or ±hh:mm
	"Z07"        Z or ±hh
	"Z070000"    Z or ±hhmmss
	"Z07:00:00"  Z or ±hh:mm:ss

Within the format string, the underscores in "\_2" and "\_\_2" represent spaces that may be replaced by digits if the following number has multiple digits, for compatibility with fixed-width Unix time formats. A leading zero represents a zero-padded value.

The formats \_\_2 and 002 are space-padded and zero-padded three-character day of year; there is no unpadded day of year format.

A comma or decimal point followed by one or more zeros represents a fractional second, printed to the given number of decimal places. A comma or decimal point followed by one or more nines represents a fractional second, printed to the given number of decimal places, with trailing zeros removed. For example "15:04:05,000" or "15:04:05.000" formats or parses with millisecond precision.

Some valid layouts are invalid time values for time.Parse, due to formats such as \_ for space padding and Z for zone information.

### const DateTime

```go
const DateTime = "2006-01-02 15:04:05"
```

### const December

```go

```

December is the twelfth month of the year.

### const February

```go

```

February is the second month of the year.

### const Friday

```go

```

Friday is the weekday after Thursday.

### const Hour

```go
const Hour = 60 * Minute
```

Common durations. There is no definition for units of Day or larger to avoid confusion across daylight savings time zone transitions.

To count the number of units in a [Duration](time.md#type-duration), divide:

	second := time.Second
	fmt.Print(int64(second/time.Millisecond)) // prints 1000

To convert an integer number of units to a Duration, multiply:

	seconds := 10
	fmt.Print(time.Duration(seconds)*time.Second) // prints 10s

### const January

```go
const January Month = 1 + iota
```

January is the first month of the year.

### const July

```go

```

July is the seventh month of the year.

### const June

```go

```

June is the sixth month of the year.

### const Kitchen

```go
const Kitchen = "3:04PM"
```

### const Layout

```go
const Layout = "01/02 03:04:05PM '06 -0700"
```

### const March

```go

```

March is the third month of the year.

### const May

```go

```

May is the fifth month of the year.

### const Microsecond

```go
const Microsecond = 1000 * Nanosecond
```

### const Millisecond

```go
const Millisecond = 1000 * Microsecond
```

### const Minute

```go
const Minute = 60 * Second
```

### const Monday

```go

```

Monday is the weekday after Sunday.

### const Nanosecond

```go
const Nanosecond Duration = 1
```

### const November

```go

```

November is the eleventh month of the year.

### const October

```go

```

October is the tenth month of the year.

### const RFC1123

```go
const RFC1123 = "Mon, 02 Jan 2006 15:04:05 MST"
```

### const RFC1123Z

```go
const RFC1123Z = "Mon, 02 Jan 2006 15:04:05 -0700"
```

### const RFC3339

```go
const RFC3339 = "2006-01-02T15:04:05Z07:00"
```

RFC1123 with numeric zone

### const RFC3339Nano

```go
const RFC3339Nano = "2006-01-02T15:04:05.999999999Z07:00"
```

### const RFC822

```go
const RFC822 = "02 Jan 06 15:04 MST"
```

### const RFC822Z

```go
const RFC822Z = "02 Jan 06 15:04 -0700"
```

### const RFC850

```go
const RFC850 = "Monday, 02-Jan-06 15:04:05 MST"
```

RFC822 with numeric zone

### const RubyDate

```go
const RubyDate = "Mon Jan 02 15:04:05 -0700 2006"
```

### const Saturday

```go

```

Saturday is the weekday after Friday.

### const Second

```go
const Second = 1000 * Millisecond
```

### const September

```go

```

September is the ninth month of the year.

### const Stamp

```go
const Stamp = "Jan _2 15:04:05"
```

Handy time stamps.

### const StampMicro

```go
const StampMicro = "Jan _2 15:04:05.000000"
```

### const StampMilli

```go
const StampMilli = "Jan _2 15:04:05.000"
```

### const StampNano

```go
const StampNano = "Jan _2 15:04:05.000000000"
```

### const Sunday

```go
const Sunday Weekday = iota
```

Sunday is the first Weekday value.

### const Thursday

```go

```

Thursday is the weekday after Wednesday.

### const TimeOnly

```go
const TimeOnly = "15:04:05"
```

### const Tuesday

```go

```

Tuesday is the weekday after Monday.

### const UnixDate

```go
const UnixDate = "Mon Jan _2 15:04:05 MST 2006"
```

### const Wednesday

```go

```

Wednesday is the weekday after Tuesday.

## Variables

### var Local

```go
var Local = &Location{name: "Local"}
```

Local is the named local location supplied by this package.

### var UTC

```go
var UTC = &Location{name: "UTC"}
```

UTC represents Universal Coordinated Time (UTC).

## Functions

### func After

```go
func After(duration Duration) <-chan Time
```

After waits for the duration to elapse and then sends the current time on the returned channel. It is equivalent to [NewTimer](time.md#func-newtimer)(d).C.

### func AfterFunc

```go
func AfterFunc(duration Duration, callback func()) *Timer
```

AfterFunc waits for the duration to elapse and then calls f in its own goroutine. It returns a [Timer](time.md#type-timer) that can be used to cancel the call using its Stop method. The returned Timer's C field is not used and will be nil.

### func Date

```go
func Date(year int, month Month, day int, hour int, minute int, second int, nanosecond int, location *Location) Time
```

Date returns the Time corresponding to

	yyyy-mm-dd hh:mm:ss + nsec nanoseconds

in the appropriate zone for that time in the given location.

The month, day, hour, min, sec, and nsec values may be outside their usual ranges and will be normalized during the conversion. For example, October 32 converts to November 1.

A daylight savings time transition skips or repeats times. For example, in the United States, March 13, 2011 2:15am never occurred, while November 6, 2011 1:15am occurred twice. In such cases, the choice of time zone, and therefore the time, is not well-defined. Date returns a time that is correct in one of the two zones involved in the transition, but it does not guarantee which.

Date panics if loc is nil.

### func FixedZone

```go
func FixedZone(name string, offset int) *Location
```

FixedZone returns a [Location](time.md#type-location) that always uses the given zone name and offset (seconds east of UTC).

### func LoadLocation

```go
func LoadLocation(name string) (*Location, error)
```

LoadLocation returns the Location with the given name.

If the name is "" or "UTC", LoadLocation returns UTC. If the name is "Local", LoadLocation returns Local.

Other names return an error.

### func LoadLocationFromTZData

```go
func LoadLocationFromTZData(name string, data []byte) (*Location, error)
```

LoadLocationFromTZData returns a location decoded from a TZif version 1, 2, or 3 data file. Input storage is not retained by the location.

### func NewTicker

```go
func NewTicker(duration Duration) *Ticker
```

NewTicker returns a Ticker that sends the current time after each positive duration interval. It panics when duration is not positive.

### func NewTimer

```go
func NewTimer(duration Duration) *Timer
```

NewTimer creates a Timer that sends the current time on a buffered channel after at least duration d.

### func Now

```go
func Now() Time
```

Now returns the current local time.

### func Parse

```go
func Parse(layout, value string) (Time, error)
```

Parse parses a formatted string and returns the time value it represents. See the documentation for the constant called [Layout](time.md#const-layout) to see how to represent the format. The second argument must be parseable using the format string (layout) provided as the first argument.

The example for [Time.Format](time.md#func-timeformat) demonstrates the working of the layout string in detail and is a good reference.

When parsing (only), the input may contain a fractional second field immediately after the seconds field, even if the layout does not signify its presence. In that case either a comma or a decimal point followed by a maximal series of digits is parsed as a fractional second. Fractional seconds are truncated to nanosecond precision.

Elements omitted from the layout are assumed to be zero or, when zero is impossible, one, so parsing "3:04pm" returns the time corresponding to Jan 1, year 0, 15:04:00 UTC (note that because the year is 0, this time is before the zero Time). Years must be in the range 0000..9999. The day of the week is checked for syntax but it is otherwise ignored.

For layouts specifying the two-digit year 06, a value NN >= 69 will be treated as 19NN and a value NN \< 69 will be treated as 20NN.

The remainder of this comment describes the handling of time zones.

In the absence of a time zone indicator, Parse returns a time in UTC.

When parsing a time with a zone offset like -0700, if the offset corresponds to a time zone used by the current location ([Local](time.md#var-local)), then Parse uses that location and zone in the returned time. Otherwise it records the time as being in a fabricated location with time fixed at the given zone offset.

When parsing a time with a zone abbreviation like MST, if the zone abbreviation has a defined offset in the current location, then that offset is used. The zone abbreviation "UTC" is recognized as UTC regardless of location. If the zone abbreviation is unknown, Parse records the time as being in a fabricated location with the given zone abbreviation and a zero offset. This choice means that such a time can be parsed and reformatted with the same layout losslessly, but the exact instant used in the representation will differ by the actual zone offset. To avoid such problems, prefer time layouts that use a numeric zone offset, or use [ParseInLocation](time.md#func-parseinlocation).

### func ParseDuration

```go
func ParseDuration(s string) (Duration, error)
```

ParseDuration parses a duration string. A duration string is a possibly signed sequence of decimal numbers, each with optional fraction and a unit suffix, such as "300ms", "-1.5h" or "2h45m". Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h".

### func ParseInLocation

```go
func ParseInLocation(layout, value string, loc *Location) (Time, error)
```

ParseInLocation is like Parse but differs in two important ways. First, in the absence of time zone information, Parse interprets a time as UTC; ParseInLocation interprets the time as in the given location. Second, when given a zone offset or abbreviation, Parse tries to match it against the Local location; ParseInLocation uses the given location.

### func Since

```go
func Since(t Time) Duration
```

Since returns the time elapsed since t. It is shorthand for time.Now().Sub(t).

### func Sleep

```go
func Sleep(duration Duration)
```

Sleep pauses the current goroutine for at least the duration d. A negative or zero duration causes Sleep to return immediately.

### func Tick

```go
func Tick(duration Duration) <-chan Time
```

Tick is a convenience wrapper for [NewTicker](time.md#func-newticker) providing access to the ticking channel only. Unlike NewTicker, Tick will return nil if d \<= 0.

### func Unix

```go
func Unix(sec int64, nsec int64) Time
```

Unix returns the local Time corresponding to the given Unix time, sec seconds and nsec nanoseconds since January 1, 1970 UTC. It is valid to pass nsec outside the range \[0, 999999999]. Not all sec values have a corresponding time value. One such value is 1\<\<63-1 (the largest int64 value).

### func UnixMicro

```go
func UnixMicro(microseconds int64) Time
```

UnixMicro returns the local Time corresponding to the given Unix time, usec microseconds since January 1, 1970 UTC.

### func UnixMilli

```go
func UnixMilli(milliseconds int64) Time
```

UnixMilli returns the local Time corresponding to the given Unix time, msec milliseconds since January 1, 1970 UTC.

### func Until

```go
func Until(t Time) Duration
```

Until returns the duration until t. It is shorthand for t.Sub(time.Now()).

## Types

### type Duration

```go
type Duration int64
```

A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.

#### func Duration.Abs

```go
func (d Duration) Abs() Duration
```

Abs returns the absolute value of d. As a special case, Duration([math.MinInt64](math.md#const-minint64)) is converted to Duration([math.MaxInt64](math.md#const-maxint64)), reducing its magnitude by 1 nanosecond.

#### func Duration.Hours

```go
func (d Duration) Hours() float64
```

Hours returns the duration as a floating point number of hours.

#### func Duration.Microseconds

```go
func (d Duration) Microseconds() int64
```

Microseconds returns the duration as an integer microsecond count.

#### func Duration.Milliseconds

```go
func (d Duration) Milliseconds() int64
```

Milliseconds returns the duration as an integer millisecond count.

#### func Duration.Minutes

```go
func (d Duration) Minutes() float64
```

Minutes returns the duration as a floating point number of minutes.

#### func Duration.Nanoseconds

```go
func (d Duration) Nanoseconds() int64
```

Nanoseconds returns the duration as an integer nanosecond count.

#### func Duration.Round

```go
func (d Duration) Round(m Duration) Duration
```

Round returns the result of rounding d to the nearest multiple of m. The rounding behavior for halfway values is to round away from zero. If the result exceeds the maximum (or minimum) value that can be stored in a [Duration](time.md#type-duration), Round returns the maximum (or minimum) duration. If m \<= 0, Round returns d unchanged.

#### func Duration.Seconds

```go
func (d Duration) Seconds() float64
```

Seconds returns the duration as a floating point number of seconds.

#### func Duration.String

```go
func (d Duration) String() string
```

String returns a string representing the duration in the form "72h3m0.5s". Leading zero units are omitted. As a special case, durations less than one second format use a smaller unit (milli-, micro-, or nanoseconds) to ensure that the leading digit is non-zero. The zero duration formats as 0s.

#### func Duration.Truncate

```go
func (d Duration) Truncate(m Duration) Duration
```

Truncate returns the result of rounding d toward zero to a multiple of m. If m \<= 0, Truncate returns d unchanged.

### type Location

```go
type Location struct { ... }
```

A Location supplies the UTC offset and daylight-saving transitions used to interpret Time values.

#### func Location.String

```go
func (l *Location) String() string
```

String returns a descriptive name for the time zone information, corresponding to the name argument to [LoadLocation](time.md#func-loadlocation) or [FixedZone](time.md#func-fixedzone).

### type Month

```go
type Month int
```

A Month specifies a month of the year (January = 1, ...).

#### func Month.String

```go
func (m Month) String() string
```

String returns the English name of the month ("January", "February", ...).

### type ParseError

```go
type ParseError struct { ... }
```

ParseError describes a problem parsing a time string.

#### field ParseError.Layout

```go
Layout string
```

Layout is the layout passed to Parse.

#### field ParseError.Value

```go
Value string
```

Value is the text passed to Parse.

#### field ParseError.LayoutElem

```go
LayoutElem string
```

LayoutElem is the layout element at the error.

#### field ParseError.ValueElem

```go
ValueElem string
```

ValueElem is the value element at the error.

#### field ParseError.Message

```go
Message string
```

Message describes the parse failure.

#### func ParseError.Error

```go
func (e *ParseError) Error() string
```

Error returns the string representation of a ParseError.

### type Ticker

```go
type Ticker struct { ... }
```

A Ticker holds a channel that delivers ticks of a clock at intervals.

#### field Ticker.C

```go
C <-chan Time
```

The channel on which the ticks are delivered.

#### func Ticker.Reset

```go
func (t *Ticker) Reset(duration Duration)
```

Reset stops a ticker and resets its period to the specified duration. The next tick will arrive after the new period elapses. The duration d must be greater than zero; if not, Reset will panic.

#### func Ticker.Stop

```go
func (t *Ticker) Stop()
```

Stop turns off a ticker. After Stop, no more ticks will be sent. Stop does not close the channel, to prevent a concurrent goroutine reading from the channel from seeing an erroneous tick.

### type Time

```go
type Time struct { ... }
```

A Time represents a wall-clock instant with nanosecond precision and an associated fixed-offset Location. Time values are safe to pass by value.

#### func Time.Add

```go
func (t Time) Add(duration Duration) Time
```

Add returns the time t+d.

#### func Time.AddDate

```go
func (t Time) AddDate(years int, months int, days int) Time
```

AddDate returns the time corresponding to adding the given number of years, months, and days to t. For example, AddDate(-1, 2, 3) applied to January 1, 2011 returns March 4, 2010.

Note that dates are fundamentally coupled to timezones, and calendrical periods like days don't have fixed durations. AddDate uses the Location of the Time value to determine these durations. That means that the same AddDate arguments can produce a different shift in absolute time depending on the base Time value and its Location. For example, AddDate(0, 0, 1) applied to 12:00 on March 27 always returns 12:00 on March 28. At some locations and in some years this is a 24 hour shift. In others it's a 23 hour shift due to daylight savings time transitions.

AddDate normalizes its result in the same way that Date does, so, for example, adding one month to October 31 yields December 1, the normalized form for November 31.

#### func Time.After

```go
func (t Time) After(other Time) bool
```

After reports whether the time instant t is after u.

#### func Time.AppendBinary

```go
func (t Time) AppendBinary(buffer []byte) ([]byte, error)
```

AppendBinary implements the [encoding.BinaryAppender](encoding.md#type-binaryappender) interface.

#### func Time.AppendFormat

```go
func (t Time) AppendFormat(b []byte, layout string) []byte
```

AppendFormat is like [Time.Format](time.md#func-timeformat) but appends the textual representation to b and returns the extended buffer.

#### func Time.AppendText

```go
func (t Time) AppendText(buffer []byte) ([]byte, error)
```

AppendText implements the [encoding.TextAppender](encoding.md#type-textappender) interface. The time is formatted in RFC 3339 format with sub-second precision. If the timestamp cannot be represented as valid RFC 3339 (e.g., the year is out of range), then an error is returned.

#### func Time.Before

```go
func (t Time) Before(other Time) bool
```

Before reports whether the time instant t is before u.

#### func Time.Clock

```go
func (t Time) Clock() (int, int, int)
```

Clock returns the hour, minute, and second within the day specified by t.

#### func Time.Compare

```go
func (t Time) Compare(other Time) int
```

Compare compares the time instant t with u. If t is before u, it returns -1; if t is after u, it returns +1; if they're the same, it returns 0.

#### func Time.Date

```go
func (t Time) Date() (int, Month, int)
```

Date returns the year, month, and day in which t occurs.

#### func Time.Day

```go
func (t Time) Day() int
```

Day returns the day of the month specified by t.

#### func Time.Equal

```go
func (t Time) Equal(other Time) bool
```

Equal reports whether t and u represent the same time instant. Two times can be equal even if they are in different locations. For example, 6:00 +0200 and 4:00 UTC are Equal. See the documentation on the Time type for the pitfalls of using == with Time values; most code should use Equal instead.

#### func Time.Format

```go
func (t Time) Format(layout string) string
```

Format returns a textual representation of the time value formatted according to the layout defined by the argument. See the documentation for the constant called [Layout](time.md#const-layout) to see how to represent the layout format.

The executable example for [Time.Format](time.md#func-timeformat) demonstrates the working of the layout string in detail and is a good reference.

#### func Time.GoString

```go
func (t Time) GoString() string
```

GoString implements [fmt.GoStringer](fmt.md#type-gostringer) and formats t to be printed in Go source code.

#### func Time.GobDecode

```go
func (t *Time) GobDecode(data []byte) error
```

GobDecode implements the gob.GobDecoder interface.

#### func Time.GobEncode

```go
func (t Time) GobEncode() ([]byte, error)
```

GobEncode implements the gob.GobEncoder interface.

#### func Time.Hour

```go
func (t Time) Hour() int
```

Hour returns the hour within the day specified by t, in the range \[0, 23].

#### func Time.ISOWeek

```go
func (t Time) ISOWeek() (int, int)
```

ISOWeek returns the ISO 8601 year and week number in which t occurs. Week ranges from 1 to 53. Jan 01 to Jan 03 of year n might belong to week 52 or 53 of year n-1, and Dec 29 to Dec 31 might belong to week 1 of year n+1.

#### func Time.In

```go
func (t Time) In(location *Location) Time
```

In returns a copy of t representing the same time instant, but with the copy's location information set to loc for display purposes.

In panics if loc is nil.

#### func Time.IsDST

```go
func (t Time) IsDST() bool
```

IsDST reports whether the time in the configured location is in Daylight Savings Time.

#### func Time.IsZero

```go
func (t Time) IsZero() bool
```

IsZero reports whether t represents the zero time instant, January 1, year 1, 00:00:00 UTC.

#### func Time.Local

```go
func (t Time) Local() Time
```

Local returns t with the location set to local time.

#### func Time.Location

```go
func (t Time) Location() *Location
```

Location returns the time zone information associated with t.

#### func Time.MarshalBinary

```go
func (t Time) MarshalBinary() ([]byte, error)
```

MarshalBinary implements the [encoding.BinaryMarshaler](encoding.md#type-binarymarshaler) interface.

#### func Time.MarshalJSON

```go
func (t Time) MarshalJSON() ([]byte, error)
```

MarshalJSON implements the [encoding/json.Marshaler](encoding/json.md#type-marshaler) interface. The time is a quoted string in the RFC 3339 format with sub-second precision. If the timestamp cannot be represented as valid RFC 3339 (e.g., the year is out of range), then an error is reported.

#### func Time.MarshalText

```go
func (t Time) MarshalText() ([]byte, error)
```

MarshalText implements the [encoding.TextMarshaler](encoding.md#type-textmarshaler) interface. The output matches that of calling the [Time.AppendText](time.md#func-timeappendtext) method.

See [Time.AppendText](time.md#func-timeappendtext) for more information.

#### func Time.Minute

```go
func (t Time) Minute() int
```

Minute returns the minute offset within the hour specified by t, in the range \[0, 59].

#### func Time.Month

```go
func (t Time) Month() Month
```

Month returns the month of the year specified by t.

#### func Time.Nanosecond

```go
func (t Time) Nanosecond() int
```

Nanosecond returns the nanosecond offset within the second specified by t, in the range \[0, 999999999].

#### func Time.Round

```go
func (t Time) Round(duration Duration) Time
```

Round returns the result of rounding t to the nearest multiple of d (since the zero time). The rounding behavior for halfway values is to round up. If d \<= 0, Round returns t unchanged.

Round operates on the time as an absolute duration since the zero time; it does not operate on the presentation form of the time. Thus, Round(Hour) may return a time with a non-zero minute, depending on the time's Location.

#### func Time.Second

```go
func (t Time) Second() int
```

Second returns the second offset within the minute specified by t, in the range \[0, 59].

#### func Time.String

```go
func (t Time) String() string
```

String returns the time formatted using the format string

	"2006-01-02 15:04:05.999999999 -0700 MST"

If the time has a monotonic clock reading, the returned string includes a final field "m=±\<value>", where value is the monotonic clock reading formatted as a decimal number of seconds.

The returned string is meant for debugging; for a stable serialized representation, use t.MarshalText, t.MarshalBinary, or t.Format with an explicit format string.

#### func Time.Sub

```go
func (t Time) Sub(other Time) Duration
```

Sub returns the duration t-u. If the result exceeds the maximum (or minimum) value that can be stored in a [Duration](time.md#type-duration), the maximum (or minimum) duration will be returned. To compute t-d for a duration d, use t.Add(-d).

#### func Time.Truncate

```go
func (t Time) Truncate(duration Duration) Time
```

Truncate returns the result of rounding t down to a multiple of d (since the zero time). If d \<= 0, Truncate returns t unchanged.

Truncate operates on the time as an absolute duration since the zero time; it does not operate on the presentation form of the time. Thus, Truncate(Hour) may return a time with a non-zero minute, depending on the time's Location.

#### func Time.UTC

```go
func (t Time) UTC() Time
```

UTC returns t with the location set to UTC.

#### func Time.Unix

```go
func (t Time) Unix() int64
```

Unix returns t as a Unix time, the number of seconds elapsed since January 1, 1970 UTC. The result does not depend on the location associated with t. Unix-like operating systems often record time as a 32-bit count of seconds, but since the method here returns a 64-bit value it is valid for billions of years into the past or future.

#### func Time.UnixMicro

```go
func (t Time) UnixMicro() int64
```

UnixMicro returns t as a Unix time, the number of microseconds elapsed since January 1, 1970 UTC. The result is undefined if the Unix time in microseconds cannot be represented by an int64 (a date before year -290307 or after year 294246). The result does not depend on the location associated with t.

#### func Time.UnixMilli

```go
func (t Time) UnixMilli() int64
```

UnixMilli returns t as a Unix time, the number of milliseconds elapsed since January 1, 1970 UTC. The result is undefined if the Unix time in milliseconds cannot be represented by an int64 (a date more than 292 million years before or after 1970). The result does not depend on the location associated with t.

#### func Time.UnixNano

```go
func (t Time) UnixNano() int64
```

UnixNano returns t as a Unix time, the number of nanoseconds elapsed since January 1, 1970 UTC. The result is undefined if the Unix time in nanoseconds cannot be represented by an int64 (a date before the year 1678 or after 2262). Note that this means the result of calling UnixNano on the zero Time is undefined. The result does not depend on the location associated with t.

#### func Time.UnmarshalBinary

```go
func (t *Time) UnmarshalBinary(data []byte) error
```

UnmarshalBinary implements the [encoding.BinaryUnmarshaler](encoding.md#type-binaryunmarshaler) interface.

#### func Time.UnmarshalJSON

```go
func (t *Time) UnmarshalJSON(data []byte) error
```

UnmarshalJSON implements the [encoding/json.Unmarshaler](encoding/json.md#type-unmarshaler) interface. The time must be a quoted string in the RFC 3339 format.

#### func Time.UnmarshalText

```go
func (t *Time) UnmarshalText(data []byte) error
```

UnmarshalText implements the [encoding.TextUnmarshaler](encoding.md#type-textunmarshaler) interface. The time must be in the RFC 3339 format.

#### func Time.Weekday

```go
func (t Time) Weekday() Weekday
```

Weekday returns the day of the week specified by t.

#### func Time.Year

```go
func (t Time) Year() int
```

Year returns the year in which t occurs.

#### func Time.YearDay

```go
func (t Time) YearDay() int
```

YearDay returns the day of the year specified by t, in the range \[1,365] for non-leap years, and \[1,366] in leap years.

#### func Time.Zone

```go
func (t Time) Zone() (string, int)
```

Zone computes the time zone in effect at time t, returning the abbreviated name of the zone (such as "CET") and its offset in seconds east of UTC.

#### func Time.ZoneBounds

```go
func (t Time) ZoneBounds() (Time, Time)
```

ZoneBounds returns the bounds of the time zone in effect at time t. The zone begins at start and the next zone begins at end. If the zone begins at the beginning of time, start will be returned as a zero Time. If the zone goes on forever, end will be returned as a zero Time. The Location of the returned times will be the same as t.

### type Timer

```go
type Timer struct { ... }
```

The Timer type represents a single event. When the Timer expires, the current time will be sent on C, unless the Timer was created by [AfterFunc](time.md#func-afterfunc). A Timer must be created with [NewTimer](time.md#func-newtimer) or AfterFunc.

#### field Timer.C

```go
C <-chan Time
```

C receives the current time when a channel-based Timer expires.

#### func Timer.Reset

```go
func (t *Timer) Reset(duration Duration) bool
```

Reset changes the timer to expire after duration d. It returns true if the timer had been active, false if the timer had expired or been stopped.

For a func-based timer created with [AfterFunc](time.md#func-afterfunc)(d, f), Reset either reschedules when f will run, in which case Reset returns true, or schedules f to run again, in which case it returns false. When Reset returns false, Reset neither waits for the prior f to complete before returning nor does it guarantee that the subsequent goroutine running f does not run concurrently with the prior one. If the caller needs to know whether the prior execution of f is completed, it must coordinate with f explicitly.

#### func Timer.Stop

```go
func (t *Timer) Stop() bool
```

Stop prevents the [Timer](time.md#type-timer) from firing. It returns true if the call stops the timer, false if the timer has already expired or been stopped.

For a func-based timer created with [AfterFunc](time.md#func-afterfunc)(d, f), if t.Stop returns false, then the timer has already expired and the function f has been started in its own goroutine; Stop does not wait for f to complete before returning. If the caller needs to know whether f is completed, it must coordinate with f explicitly.

### type Weekday

```go
type Weekday int
```

A Weekday specifies a day of the week (Sunday = 0, ...).

#### func Weekday.String

```go
func (d Weekday) String() string
```

String returns the English name of the day ("Sunday", "Monday", ...).
