<!-- Generated by mini-go doc. Target: minigo. -->

# Package `strings`

```go
package strings
```

Package strings implements simple functions to manipulate UTF-8 encoded strings.

For information about UTF-8 strings in Go, see [https://blog.golang.org/strings](https://blog.golang.org/strings).

## Index

- [type Builder](#type-builder)
  - [Builder.Cap](#func-buildercap)
  - [Builder.Grow](#func-buildergrow)
  - [Builder.Len](#func-builderlen)
  - [Builder.Reset](#func-builderreset)
  - [Builder.String](#func-builderstring)
  - [Builder.Write](#func-builderwrite)
  - [Builder.WriteByte](#func-builderwritebyte)
  - [Builder.WriteRune](#func-builderwriterune)
  - [Builder.WriteString](#func-builderwritestring)
- [func Clone](#func-clone)
- [func Compare](#func-compare)
- [func Contains](#func-contains)
- [func ContainsAny](#func-containsany)
- [func ContainsFunc](#func-containsfunc)
- [func ContainsRune](#func-containsrune)
- [func Count](#func-count)
- [func Cut](#func-cut)
- [func CutPrefix](#func-cutprefix)
- [func CutSuffix](#func-cutsuffix)
- [func EqualFold](#func-equalfold)
- [func Fields](#func-fields)
- [func FieldsFunc](#func-fieldsfunc)
- [func FieldsFuncSeq](#func-fieldsfuncseq)
- [func FieldsSeq](#func-fieldsseq)
- [func HasPrefix](#func-hasprefix)
- [func HasSuffix](#func-hassuffix)
- [func Index](#func-index)
- [func IndexAny](#func-indexany)
- [func IndexByte](#func-indexbyte)
- [func IndexFunc](#func-indexfunc)
- [func IndexRune](#func-indexrune)
- [func Join](#func-join)
- [func LastIndex](#func-lastindex)
- [func LastIndexAny](#func-lastindexany)
- [func LastIndexByte](#func-lastindexbyte)
- [func LastIndexFunc](#func-lastindexfunc)
- [func Lines](#func-lines)
- [func Map](#func-map)
- [func NewReader](#func-newreader)
- [func NewReplacer](#func-newreplacer)
- [type Reader](#type-reader)
  - [Reader.Len](#func-readerlen)
  - [Reader.Read](#func-readerread)
  - [Reader.ReadAt](#func-readerreadat)
  - [Reader.ReadByte](#func-readerreadbyte)
  - [Reader.ReadRune](#func-readerreadrune)
  - [Reader.Reset](#func-readerreset)
  - [Reader.Seek](#func-readerseek)
  - [Reader.Size](#func-readersize)
  - [Reader.UnreadByte](#func-readerunreadbyte)
  - [Reader.UnreadRune](#func-readerunreadrune)
  - [Reader.WriteTo](#func-readerwriteto)
- [func Repeat](#func-repeat)
- [func Replace](#func-replace)
- [func ReplaceAll](#func-replaceall)
- [type Replacer](#type-replacer)
  - [Replacer.Replace](#func-replacerreplace)
  - [Replacer.WriteString](#func-replacerwritestring)
- [func Split](#func-split)
- [func SplitAfter](#func-splitafter)
- [func SplitAfterN](#func-splitaftern)
- [func SplitAfterSeq](#func-splitafterseq)
- [func SplitN](#func-splitn)
- [func SplitSeq](#func-splitseq)
- [func ToLower](#func-tolower)
- [func ToTitle](#func-totitle)
- [func ToUpper](#func-toupper)
- [func ToValidUTF8](#func-tovalidutf8)
- [func Trim](#func-trim)
- [func TrimFunc](#func-trimfunc)
- [func TrimLeft](#func-trimleft)
- [func TrimLeftFunc](#func-trimleftfunc)
- [func TrimPrefix](#func-trimprefix)
- [func TrimRight](#func-trimright)
- [func TrimRightFunc](#func-trimrightfunc)
- [func TrimSpace](#func-trimspace)
- [func TrimSuffix](#func-trimsuffix)

## Functions

### func Clone

```go
func Clone(s string) string
```

Clone returns a fresh copy of s.

### func Compare

```go
func Compare(a string, b string) int
```

Compare returns an integer comparing two strings lexicographically. The result will be 0 if a == b, -1 if a \< b, and +1 if a > b.

Use Compare when you need to perform a three-way comparison (with [slices.SortFunc](slices.md#func-sortfunc), for example). It is usually clearer and always faster to use the built-in string comparison operators ==, \<, >, and so on.

### func Contains

```go
func Contains(s string, substr string) bool
```

Contains reports whether substr is within s.

### func ContainsAny

```go
func ContainsAny(s string, chars string) bool
```

ContainsAny reports whether any Unicode code points in chars are within s.

### func ContainsFunc

```go
func ContainsFunc(s string, f func(rune) bool) bool
```

ContainsFunc reports whether any Unicode code point r within s satisfies f(r).

### func ContainsRune

```go
func ContainsRune(s string, r rune) bool
```

ContainsRune reports whether the Unicode code point r is within s.

### func Count

```go
func Count(s string, substr string) int
```

Count counts the number of non-overlapping instances of substr in s. If substr is an empty string, Count returns 1 + the number of Unicode code points in s.

### func Cut

```go
func Cut(s string, sep string) (string, string, bool)
```

Cut slices s around the first instance of sep, returning the text before and after sep. The found result reports whether sep appears in s. If sep does not appear in s, cut returns s, "", false.

### func CutPrefix

```go
func CutPrefix(s string, prefix string) (string, bool)
```

CutPrefix returns s without the provided leading prefix string and reports whether it found the prefix. If s doesn't start with prefix, CutPrefix returns s, false. If prefix is the empty string, CutPrefix returns s, true.

### func CutSuffix

```go
func CutSuffix(s string, suffix string) (string, bool)
```

CutSuffix returns s without the provided ending suffix string and reports whether it found the suffix. If s doesn't end with suffix, CutSuffix returns s, false. If suffix is the empty string, CutSuffix returns s, true.

### func EqualFold

```go
func EqualFold(s string, t string) bool
```

EqualFold reports whether s and t, interpreted as UTF-8 strings, are equal under simple Unicode case-folding, which is a more general form of case-insensitivity.

### func Fields

```go
func Fields(s string) []string
```

Fields splits the string s around each instance of one or more consecutive white space characters, as defined by [unicode.IsSpace](unicode.md#func-isspace), returning a slice of substrings of s or an empty slice if s contains only white space. Every element of the returned slice is non-empty. Unlike [Split](strings.md#func-split), leading and trailing runs of white space characters are discarded.

### func FieldsFunc

```go
func FieldsFunc(s string, f func(rune) bool) []string
```

FieldsFunc splits the string s at each run of Unicode code points c satisfying f(c) and returns an array of slices of s. If all code points in s satisfy f(c) or the string is empty, an empty slice is returned. Every element of the returned slice is non-empty. Unlike [Split](strings.md#func-split), leading and trailing runs of code points satisfying f(c) are discarded.

FieldsFunc makes no guarantees about the order in which it calls f(c) and assumes that f always returns the same value for a given c.

### func FieldsFuncSeq

```go
func FieldsFuncSeq(s string, f func(rune) bool) iter.Seq[string]
```

FieldsFuncSeq returns an iterator over fields separated by runs of code points satisfying f.

### func FieldsSeq

```go
func FieldsSeq(s string) iter.Seq[string]
```

FieldsSeq returns an iterator over the fields of s separated by Unicode white space.

### func HasPrefix

```go
func HasPrefix(s string, prefix string) bool
```

HasPrefix reports whether the string s begins with prefix.

### func HasSuffix

```go
func HasSuffix(s string, suffix string) bool
```

HasSuffix reports whether the string s ends with suffix.

### func Index

```go
func Index(s string, substr string) int
```

Index returns the index of the first instance of substr in s, or -1 if substr is not present in s.

### func IndexAny

```go
func IndexAny(s string, chars string) int
```

IndexAny returns the index of the first instance of any Unicode code point from chars in s, or -1 if no Unicode code point from chars is present in s.

### func IndexByte

```go
func IndexByte(s string, c byte) int
```

IndexByte returns the index of the first instance of c in s, or -1 if c is not present in s.

### func IndexFunc

```go
func IndexFunc(s string, f func(rune) bool) int
```

IndexFunc returns the index into s of the first Unicode code point satisfying f(c), or -1 if none do.

### func IndexRune

```go
func IndexRune(s string, r rune) int
```

IndexRune returns the index of the first instance of the Unicode code point r, or -1 if rune is not present in s. If r is [utf8.RuneError](unicode/utf8.md#const-runeerror), it returns the first instance of any invalid UTF-8 byte sequence.

### func Join

```go
func Join(elems []string, sep string) string
```

Join concatenates the elements of its first argument to create a single string. The separator string sep is placed between elements in the resulting string.

### func LastIndex

```go
func LastIndex(s string, substr string) int
```

LastIndex returns the index of the last instance of substr in s, or -1 if substr is not present in s.

### func LastIndexAny

```go
func LastIndexAny(s string, chars string) int
```

LastIndexAny returns the index of the last instance of any Unicode code point from chars in s, or -1 if no code point is present.

### func LastIndexByte

```go
func LastIndexByte(s string, c byte) int
```

LastIndexByte returns the index of the last instance of c in s, or -1 if c is not present in s.

### func LastIndexFunc

```go
func LastIndexFunc(s string, f func(rune) bool) int
```

LastIndexFunc returns the index into s of the last Unicode code point satisfying f(c), or -1 if none do.

### func Lines

```go
func Lines(s string) iter.Seq[string]
```

Lines returns an iterator over the newline-terminated lines in s. The final

### func Map

```go
func Map(mapping func(rune) rune, s string) string
```

Map returns a copy of the string s with all its characters modified according to the mapping function. If mapping returns a negative value, the character is dropped from the string with no replacement.

### func NewReader

```go
func NewReader(s string) *Reader
```

NewReader returns a new [Reader](strings.md#type-reader) reading from s. It is similar to [bytes.NewBufferString](bytes.md#func-newbufferstring) but more efficient and non-writable.

### func NewReplacer

```go
func NewReplacer(oldnew ...string) *Replacer
```

NewReplacer returns a new Replacer from alternating old, new string pairs. NewReplacer panics when oldnew has odd length.

### func Repeat

```go
func Repeat(s string, count int) string
```

Repeat returns a new string consisting of count copies of the string s.

It panics if count is negative or if the result of (len(s) \* count) overflows.

### func Replace

```go
func Replace(s string, old string, new string, n int) string
```

Replace returns a copy of the string s with the first n non-overlapping instances of old replaced by new. If old is empty, it matches at the beginning of the string and after each UTF-8 sequence, yielding up to k+1 replacements for a k-rune string. If n \< 0, there is no limit on the number of replacements.

### func ReplaceAll

```go
func ReplaceAll(s string, old string, new string) string
```

ReplaceAll returns a copy of the string s with all non-overlapping instances of old replaced by new. If old is empty, it matches at the beginning of the string and after each UTF-8 sequence, yielding up to k+1 replacements for a k-rune string.

### func Split

```go
func Split(s string, sep string) []string
```

Split slices s into all substrings separated by sep and returns a slice of the substrings between those separators.

If s does not contain sep and sep is not empty, Split returns a slice of length 1 whose only element is s.

If sep is empty, Split splits after each UTF-8 sequence. If both s and sep are empty, Split returns an empty slice.

It is equivalent to [SplitN](strings.md#func-splitn) with a count of -1.

To split around the first instance of a separator, see [Cut](strings.md#func-cut).

### func SplitAfter

```go
func SplitAfter(s string, sep string) []string
```

SplitAfter slices s into all substrings after each instance of sep and returns a slice of those substrings.

If s does not contain sep and sep is not empty, SplitAfter returns a slice of length 1 whose only element is s.

If sep is empty, SplitAfter splits after each UTF-8 sequence. If both s and sep are empty, SplitAfter returns an empty slice.

It is equivalent to [SplitAfterN](strings.md#func-splitaftern) with a count of -1.

### func SplitAfterN

```go
func SplitAfterN(s string, sep string, n int) []string
```

SplitAfterN slices s into substrings after each instance of sep and returns a slice of those substrings.

The count determines the number of substrings to return:

  - n > 0: at most n substrings; the last substring will be the unsplit remainder;
  - n == 0: the result is nil (zero substrings);
  - n \< 0: all substrings.

Edge cases for s and sep (for example, empty strings) are handled as described in the documentation for [SplitAfter](strings.md#func-splitafter).

### func SplitAfterSeq

```go
func SplitAfterSeq(s string, sep string) iter.Seq[string]
```

SplitAfterSeq returns an iterator over substrings of s split after sep.

### func SplitN

```go
func SplitN(s string, sep string, n int) []string
```

SplitN slices s into substrings separated by sep and returns a slice of the substrings between those separators.

The count determines the number of substrings to return:

  - n > 0: at most n substrings; the last substring will be the unsplit remainder;
  - n == 0: the result is nil (zero substrings);
  - n \< 0: all substrings.

Edge cases for s and sep (for example, empty strings) are handled as described in the documentation for [Split](strings.md#func-split).

To split around the first instance of a separator, see [Cut](strings.md#func-cut).

### func SplitSeq

```go
func SplitSeq(s string, sep string) iter.Seq[string]
```

SplitSeq returns an iterator over all substrings of s separated by sep.

### func ToLower

```go
func ToLower(s string) string
```

ToLower returns s with all Unicode letters mapped to their lower case.

### func ToTitle

```go
func ToTitle(s string) string
```

ToTitle returns a copy of the string s with all Unicode letters mapped to their Unicode title case.

### func ToUpper

```go
func ToUpper(s string) string
```

ToUpper returns s with all Unicode letters mapped to their upper case.

### func ToValidUTF8

```go
func ToValidUTF8(s string, replacement string) string
```

ToValidUTF8 returns a copy of s with each run of invalid UTF-8 byte sequences replaced by replacement, which may be empty.

### func Trim

```go
func Trim(s string, cutset string) string
```

Trim returns a slice of the string s with all leading and trailing Unicode code points contained in cutset removed.

### func TrimFunc

```go
func TrimFunc(s string, f func(rune) bool) string
```

TrimFunc returns a slice of the string s with all leading and trailing Unicode code points c satisfying f(c) removed.

### func TrimLeft

```go
func TrimLeft(s string, cutset string) string
```

TrimLeft returns a slice of the string s with all leading Unicode code points contained in cutset removed.

To remove a prefix, use [TrimPrefix](strings.md#func-trimprefix) instead.

### func TrimLeftFunc

```go
func TrimLeftFunc(s string, f func(rune) bool) string
```

TrimLeftFunc returns a slice of the string s with all leading Unicode code points c satisfying f(c) removed.

### func TrimPrefix

```go
func TrimPrefix(s string, prefix string) string
```

TrimPrefix returns s without the provided leading prefix string. If s doesn't start with prefix, s is returned unchanged.

### func TrimRight

```go
func TrimRight(s string, cutset string) string
```

TrimRight returns a slice of the string s, with all trailing Unicode code points contained in cutset removed.

To remove a suffix, use [TrimSuffix](strings.md#func-trimsuffix) instead.

### func TrimRightFunc

```go
func TrimRightFunc(s string, f func(rune) bool) string
```

TrimRightFunc returns a slice of the string s with all trailing Unicode code points c satisfying f(c) removed.

### func TrimSpace

```go
func TrimSpace(s string) string
```

TrimSpace returns a slice (substring) of the string s, with all leading and trailing white space removed, as defined by Unicode.

### func TrimSuffix

```go
func TrimSuffix(s string, suffix string) string
```

TrimSuffix returns s without the provided trailing suffix string. If s doesn't end with suffix, s is returned unchanged.

## Types

### type Builder

```go
type Builder struct { ... }
```

A Builder is used to efficiently build a string using [Builder.Write](strings.md#func-builderwrite) methods. It minimizes memory copying. The zero value is ready to use. Do not copy a non-zero Builder.

#### func Builder.Cap

```go
func (b *Builder) Cap() int
```

Cap returns the capacity of the builder's underlying byte slice. It is the total space allocated for the string being built and includes any bytes already written.

#### func Builder.Grow

```go
func (b *Builder) Grow(n int)
```

Grow grows b's capacity, if necessary, to guarantee space for another n bytes. After Grow(n), at least n bytes can be written to b without another allocation. If n is negative, Grow panics.

#### func Builder.Len

```go
func (b *Builder) Len() int
```

Len returns the number of accumulated bytes; b.Len() == len(b.String()).

#### func Builder.Reset

```go
func (b *Builder) Reset()
```

Reset resets the [Builder](strings.md#type-builder) to be empty.

#### func Builder.String

```go
func (b *Builder) String() string
```

String returns the accumulated string.

#### func Builder.Write

```go
func (b *Builder) Write(p []byte) (int, error)
```

Write appends the contents of p to b's buffer. Write always returns len(p), nil.

#### func Builder.WriteByte

```go
func (b *Builder) WriteByte(c byte) error
```

WriteByte appends the byte c to b's buffer. The returned error is always nil.

#### func Builder.WriteRune

```go
func (b *Builder) WriteRune(r rune) (int, error)
```

WriteRune appends the UTF-8 encoding of Unicode code point r to b's buffer. It returns the length of r and a nil error.

#### func Builder.WriteString

```go
func (b *Builder) WriteString(s string) (int, error)
```

WriteString appends the contents of s to b's buffer. It returns the length of s and a nil error.

### type Reader

```go
type Reader struct { ... }
```

A Reader implements the [io.Reader](io.md#type-reader), [io.ReaderAt](io.md#type-readerat), [io.ByteReader](io.md#type-bytereader), [io.ByteScanner](io.md#type-bytescanner), [io.RuneReader](io.md#type-runereader), [io.RuneScanner](io.md#type-runescanner), [io.Seeker](io.md#type-seeker), and [io.WriterTo](io.md#type-writerto) interfaces by reading from a string. The zero value for Reader operates like a Reader of an empty string.

#### func Reader.Len

```go
func (r *Reader) Len() int
```

Len returns the number of bytes of the unread portion of the string.

#### func Reader.Read

```go
func (r *Reader) Read(p []byte) (int, error)
```

Read implements the [io.Reader](io.md#type-reader) interface.

#### func Reader.ReadAt

```go
func (r *Reader) ReadAt(p []byte, offset int64) (int, error)
```

ReadAt implements the [io.ReaderAt](io.md#type-readerat) interface.

#### func Reader.ReadByte

```go
func (r *Reader) ReadByte() (byte, error)
```

ReadByte implements the [io.ByteReader](io.md#type-bytereader) interface.

#### func Reader.ReadRune

```go
func (r *Reader) ReadRune() (rune, int, error)
```

ReadRune implements the [io.RuneReader](io.md#type-runereader) interface.

#### func Reader.Reset

```go
func (r *Reader) Reset(s string)
```

Reset resets the [Reader](strings.md#type-reader) to be reading from s.

#### func Reader.Seek

```go
func (r *Reader) Seek(offset int64, whence int) (int64, error)
```

Seek implements the [io.Seeker](io.md#type-seeker) interface.

#### func Reader.Size

```go
func (r *Reader) Size() int64
```

Size returns the original length of the underlying string. Size is the number of bytes available for reading via [Reader.ReadAt](strings.md#func-readerreadat). The returned value is always the same and is not affected by calls to any other method.

#### func Reader.UnreadByte

```go
func (r *Reader) UnreadByte() error
```

UnreadByte implements the [io.ByteScanner](io.md#type-bytescanner) interface.

#### func Reader.UnreadRune

```go
func (r *Reader) UnreadRune() error
```

UnreadRune implements the [io.RuneScanner](io.md#type-runescanner) interface.

#### func Reader.WriteTo

```go
func (r *Reader) WriteTo(w io.Writer) (int64, error)
```

WriteTo implements the [io.WriterTo](io.md#type-writerto) interface.

### type Replacer

```go
type Replacer struct { ... }
```

Replacer replaces a list of strings with replacements. It is safe for concurrent use after construction.

#### func Replacer.Replace

```go
func (r *Replacer) Replace(s string) string
```

Replace returns a copy of s with all replacements performed. Matches at the same position use the first pair passed to NewReplacer.

#### func Replacer.WriteString

```go
func (r *Replacer) WriteString(w io.Writer, s string) (int, error)
```

WriteString writes the replaced form of s to w.
