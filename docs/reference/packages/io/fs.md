<!-- Generated by mini-go doc. Target: minigo. -->

# Package `io/fs`

```go
package fs
```

Package fs defines basic interfaces to a file system. A file system can be provided by the host operating system but also by other packages.

#### Path Names

The interfaces in this package all operate on the same path name syntax, regardless of the host operating system.

Path names are UTF-8-encoded, unrooted, slash-separated sequences of path elements, like “x/y/z”. Path names must not contain an element that is “.” or “..” or the empty string, except for the special case that the name "." may be used for the root directory. Paths must not start or end with a slash: “/x” and “x/” are invalid.

#### Testing

See the [testing/fstest](../testing/fstest.md) package for support with testing implementations of file systems.

## Index

- [type DirEntry](#type-direntry)
- [var ErrClosed](#var-errclosed)
- [var ErrExist](#var-errexist)
- [var ErrInvalid](#var-errinvalid)
- [var ErrNotExist](#var-errnotexist)
- [var ErrPermission](#var-errpermission)
- [type FS](#type-fs)
- [type File](#type-file)
- [type FileInfo](#type-fileinfo)
- [func FileInfoToDirEntry](#func-fileinfotodirentry)
- [type FileMode](#type-filemode)
  - [FileMode.IsDir](#func-filemodeisdir)
  - [FileMode.IsRegular](#func-filemodeisregular)
  - [FileMode.Perm](#func-filemodeperm)
  - [FileMode.String](#func-filemodestring)
  - [FileMode.Type](#func-filemodetype)
- [func FormatDirEntry](#func-formatdirentry)
- [func FormatFileInfo](#func-formatfileinfo)
- [func Glob](#func-glob)
- [type GlobFS](#type-globfs)
- [func Lstat](#func-lstat)
- [const ModeAppend](#const-modeappend)
- [const ModeCharDevice](#const-modechardevice)
- [const ModeDevice](#const-modedevice)
- [const ModeDir](#const-modedir)
- [const ModeExclusive](#const-modeexclusive)
- [const ModeIrregular](#const-modeirregular)
- [const ModeNamedPipe](#const-modenamedpipe)
- [const ModePerm](#const-modeperm)
- [const ModeSetgid](#const-modesetgid)
- [const ModeSetuid](#const-modesetuid)
- [const ModeSocket](#const-modesocket)
- [const ModeSticky](#const-modesticky)
- [const ModeSymlink](#const-modesymlink)
- [const ModeTemporary](#const-modetemporary)
- [const ModeType](#const-modetype)
- [type PathError](#type-patherror)
  - [PathError.Error](#func-patherrorerror)
  - [PathError.Timeout](#func-patherrortimeout)
  - [PathError.Unwrap](#func-patherrorunwrap)
- [func ReadDir](#func-readdir)
- [type ReadDirFS](#type-readdirfs)
- [type ReadDirFile](#type-readdirfile)
- [func ReadFile](#func-readfile)
- [type ReadFileFS](#type-readfilefs)
- [func ReadLink](#func-readlink)
- [type ReadLinkFS](#type-readlinkfs)
- [var SkipAll](#var-skipall)
- [var SkipDir](#var-skipdir)
- [func Stat](#func-stat)
- [type StatFS](#type-statfs)
- [func Sub](#func-sub)
- [type SubFS](#type-subfs)
- [func ValidPath](#func-validpath)
- [func WalkDir](#func-walkdir)
- [type WalkDirFunc](#type-walkdirfunc)

## Constants

### const ModeAppend

```go

```

The defined file mode bits are the most significant bits of the [FileMode](fs.md#type-filemode). The nine least-significant bits are the standard Unix rwxrwxrwx permissions. The values of these bits should be considered part of the public API and may be used in wire protocols or disk representations: they must not be changed, although new bits might be added.

### const ModeCharDevice

```go

```

### const ModeDevice

```go

```

### const ModeDir

```go
const ModeDir FileMode = 1 << (32 - 1 - iota)
```

The single letters are the abbreviations used by the String method's formatting.

### const ModeExclusive

```go

```

### const ModeIrregular

```go

```

### const ModeNamedPipe

```go

```

### const ModePerm

```go
const ModePerm FileMode = 0777
```

### const ModeSetgid

```go

```

### const ModeSetuid

```go

```

### const ModeSocket

```go

```

### const ModeSticky

```go

```

### const ModeSymlink

```go

```

### const ModeTemporary

```go

```

### const ModeType

```go
const ModeType = ModeDir | ModeSymlink | ModeNamedPipe | ModeSocket | ModeDevice | ModeCharDevice | ModeIrregular
```

Mask for the type bits. For regular files, none will be set.

## Variables

### var ErrClosed

```go
var ErrClosed = errors.New("file already closed")
```

Generic file system errors. Errors returned by file systems can be tested against these errors using [errors.Is](../errors.md#func-is).

### var ErrExist

```go
var ErrExist = errors.New("file already exists")
```

### var ErrInvalid

```go
var ErrInvalid = errors.New("invalid argument")
```

### var ErrNotExist

```go
var ErrNotExist = errors.New("file does not exist")
```

### var ErrPermission

```go
var ErrPermission = errors.New("permission denied")
```

### var SkipAll

```go
var SkipAll = errors.New("skip everything and stop the walk")
```

SkipAll is used as a return value from [WalkDirFunc](fs.md#type-walkdirfunc) to indicate that all remaining files and directories are to be skipped. It is not returned as an error by any function.

### var SkipDir

```go
var SkipDir = errors.New("skip this directory")
```

SkipDir is used as a return value from [WalkDirFunc](fs.md#type-walkdirfunc) to indicate that the directory named in the call is to be skipped. It is not returned as an error by any function.

## Functions

### func FileInfoToDirEntry

```go
func FileInfoToDirEntry(info FileInfo) DirEntry
```

FileInfoToDirEntry returns a [DirEntry](fs.md#type-direntry) that returns information from info. If info is nil, FileInfoToDirEntry returns nil.

### func FormatDirEntry

```go
func FormatDirEntry(entry DirEntry) string
```

FormatDirEntry returns a formatted version of dir for human readability. Implementations of [DirEntry](fs.md#type-direntry) can call this from a String method. The outputs for a directory named subdir and a file named hello.go are:

	d subdir/
	- hello.go

### func FormatFileInfo

```go
func FormatFileInfo(info FileInfo) string
```

FormatFileInfo returns a formatted version of info for human readability. Implementations of [FileInfo](fs.md#type-fileinfo) can call this from a String method. The output for a file named "hello.go", 100 bytes, mode 0o644, created January 1, 1970 at noon is

	-rw-r--r-- 100 1970-01-01 12:00:00 hello.go

### func Glob

```go
func Glob(filesystem FS, pattern string) ([]string, error)
```

Glob returns the names of all files matching pattern or nil if there is no matching file. The syntax of patterns is the same as in [path.Match](../path.md#func-match). The pattern may describe hierarchical names such as usr/\*/bin/ed.

Glob ignores file system errors such as I/O errors reading directories. The only possible returned error is [path.ErrBadPattern](../path.md#var-errbadpattern), reporting that the pattern is malformed.

If fs implements [GlobFS](fs.md#type-globfs), Glob calls fs.Glob. Otherwise, Glob uses [ReadDir](fs.md#func-readdir) to traverse the directory tree and look for matches for the pattern.

### func Lstat

```go
func Lstat(filesystem FS, name string) (FileInfo, error)
```

Lstat returns a [FileInfo](fs.md#type-fileinfo) describing the named file. If the file is a symbolic link, the returned [FileInfo](fs.md#type-fileinfo) describes the symbolic link. Lstat makes no attempt to follow the link.

If fsys does not implement [ReadLinkFS](fs.md#type-readlinkfs), then Lstat is identical to [Stat](fs.md#func-stat).

### func ReadDir

```go
func ReadDir(filesystem FS, name string) ([]DirEntry, error)
```

ReadDir reads the named directory and returns a list of directory entries sorted by filename.

If fs implements [ReadDirFS](fs.md#type-readdirfs), ReadDir calls fs.ReadDir. Otherwise ReadDir calls fs.Open and uses ReadDir and Close on the returned file.

### func ReadFile

```go
func ReadFile(filesystem FS, name string) ([]byte, error)
```

ReadFile reads the named file from the file system fs and returns its contents. A successful call returns a nil error, not [io.EOF](../io.md#var-eof). (Because ReadFile reads the whole file, the expected EOF from the final Read is not treated as an error to be reported.)

If fs implements [ReadFileFS](fs.md#type-readfilefs), ReadFile calls fs.ReadFile. Otherwise ReadFile calls fs.Open and uses Read and Close on the returned [File](fs.md#type-file).

### func ReadLink

```go
func ReadLink(filesystem FS, name string) (string, error)
```

ReadLink returns the destination of the named symbolic link.

If fsys does not implement [ReadLinkFS](fs.md#type-readlinkfs), then ReadLink returns an error.

### func Stat

```go
func Stat(filesystem FS, name string) (FileInfo, error)
```

Stat returns a [FileInfo](fs.md#type-fileinfo) describing the named file from the file system.

If fs implements [StatFS](fs.md#type-statfs), Stat calls fs.Stat. Otherwise, Stat opens the [File](fs.md#type-file) to stat it.

### func Sub

```go
func Sub(filesystem FS, dir string) (FS, error)
```

Sub returns an [FS](fs.md#type-fs) corresponding to the subtree rooted at fsys's dir.

If dir is ".", Sub returns fsys unchanged. Otherwise, if fs implements [SubFS](fs.md#type-subfs), Sub returns fsys.Sub(dir). Otherwise, Sub returns a new [FS](fs.md#type-fs) implementation sub that, in effect, implements sub.Open(name) as fsys.Open(path.Join(dir, name)). The implementation also translates calls to ReadDir, ReadFile, ReadLink, Lstat, and Glob appropriately.

Note that Sub(os.DirFS("/"), "prefix") is equivalent to os.DirFS("/prefix") and that neither of them guarantees to avoid operating system accesses outside "/prefix", because the implementation of [os.DirFS](../os.md#dirfs) does not check for symbolic links inside "/prefix" that point to other directories. That is, [os.DirFS](../os.md#dirfs) is not a general substitute for a chroot-style security mechanism, and Sub does not change that fact.

### func ValidPath

```go
func ValidPath(name string) bool
```

ValidPath reports whether the given path name is valid for use in a call to Open.

Note that paths are slash-separated on all systems, even Windows. Paths containing other characters such as backslash and colon are accepted as valid, but those characters must never be interpreted by an [FS](fs.md#type-fs) implementation as path element separators. See the [Path Names](https://pkg.go.dev/io/fs#hdr-Path_Names) section for more details.

### func WalkDir

```go
func WalkDir(filesystem FS, root string, callback WalkDirFunc) error
```

WalkDir walks the file tree rooted at root, calling fn for each file or directory in the tree, including root.

All errors that arise visiting files and directories are filtered by fn: see the [fs.WalkDirFunc](fs.md#type-walkdirfunc) documentation for details.

The files are walked in lexical order, which makes the output deterministic but requires WalkDir to read an entire directory into memory before proceeding to walk that directory.

WalkDir does not follow symbolic links found in directories, but if root itself is a symbolic link, its target will be walked.

## Types

### type DirEntry

```go
type DirEntry interface { ... }
```

A DirEntry is an entry read from a directory (using the [ReadDir](fs.md#func-readdir) function or a [ReadDirFile](fs.md#type-readdirfile)'s ReadDir method).

#### method DirEntry.Name

```go
Name() string
```

Name returns the name of the file (or subdirectory) described by the entry. This name is only the final element of the path (the base name), not the entire path. For example, Name would return "hello.go" not "home/gopher/hello.go".

#### method DirEntry.IsDir

```go
IsDir() bool
```

IsDir reports whether the entry describes a directory.

#### method DirEntry.Type

```go
Type() FileMode
```

Type returns the type bits for the entry. The type bits are a subset of the usual FileMode bits, those returned by the FileMode.Type method.

#### method DirEntry.Info

```go
Info() (FileInfo, error)
```

Info returns the FileInfo for the file or subdirectory described by the entry. The returned FileInfo may be from the time of the original directory read or from the time of the call to Info. If the file has been removed or renamed since the directory read, Info may return an error satisfying errors.Is(err, ErrNotExist). If the entry denotes a symbolic link, Info reports the information about the link itself, not the link's target.

### type FS

```go
type FS interface { ... }
```

An FS provides access to a hierarchical file system.

The FS interface is the minimum implementation required of the file system. A file system may implement additional interfaces, such as [ReadFileFS](fs.md#type-readfilefs), to provide additional or optimized functionality.

[testing/fstest.TestFS](../testing/fstest.md#func-testfs) may be used to test implementations of an FS for correctness.

#### method FS.Open

```go
Open(name string) (File, error)
```

Open opens the named file. \[File.Close] must be called to release any associated resources.

When Open returns an error, it should be of type \*PathError with the Op field set to "open", the Path field set to name, and the Err field describing the problem.

Open should reject attempts to open names that do not satisfy ValidPath(name), returning a \*PathError with Err set to ErrInvalid or ErrNotExist.

### type File

```go
type File interface { ... }
```

A File provides access to a single file. The File interface is the minimum implementation required of the file. Directory files should also implement [ReadDirFile](fs.md#type-readdirfile). A file may implement [io.ReaderAt](../io.md#type-readerat) or [io.Seeker](../io.md#type-seeker) as optimizations.

#### method File.Stat

```go
Stat() (FileInfo, error)
```

Stat returns information describing the file.

#### method File.Read

```go
Read([]byte) (int, error)
```

Read reads up to len(p) bytes from the file.

#### method File.Close

```go
Close() error
```

Close closes the file and releases its associated state.

### type FileInfo

```go
type FileInfo interface { ... }
```

A FileInfo describes a file and is returned by [Stat](fs.md#func-stat).

#### method FileInfo.Name

```go
Name() string
```

base name of the file

#### method FileInfo.Size

```go
Size() int64
```

length in bytes for regular files; system-dependent for others

#### method FileInfo.Mode

```go
Mode() FileMode
```

file mode bits

#### method FileInfo.ModTime

```go
ModTime() time.Time
```

modification time

#### method FileInfo.IsDir

```go
IsDir() bool
```

abbreviation for Mode().IsDir()

#### method FileInfo.Sys

```go
Sys() any
```

underlying data source (can return nil)

### type FileMode

```go
type FileMode uint32
```

A FileMode represents a file's mode and permission bits. The bits have the same definition on all systems, so that information about files can be moved from one system to another portably. Not all bits apply to all systems. The only required bit is [ModeDir](fs.md#const-modedir) for directories.

#### func FileMode.IsDir

```go
func (m FileMode) IsDir() bool
```

IsDir reports whether m describes a directory. That is, it tests for the [ModeDir](fs.md#const-modedir) bit being set in m.

#### func FileMode.IsRegular

```go
func (m FileMode) IsRegular() bool
```

IsRegular reports whether m describes a regular file. That is, it tests that no mode type bits are set.

#### func FileMode.Perm

```go
func (m FileMode) Perm() FileMode
```

Perm returns the Unix permission bits in m (m & [ModePerm](fs.md#const-modeperm)).

#### func FileMode.String

```go
func (m FileMode) String() string
```

String returns the textual representation of the file mode.

#### func FileMode.Type

```go
func (m FileMode) Type() FileMode
```

Type returns type bits in m (m & [ModeType](fs.md#const-modetype)).

### type GlobFS

```go
type GlobFS interface { ... }
```

A GlobFS is a file system with a Glob method.

#### method GlobFS.Glob

```go
Glob(pattern string) ([]string, error)
```

Glob returns the names of all files matching pattern, providing an implementation of the top-level Glob function.

### type PathError

```go
type PathError struct { ... }
```

PathError records an error and the operation and file path that caused it.

#### field PathError.Op

```go
Op string
```

Op is the operation that failed.

#### field PathError.Path

```go
Path string
```

Path is the path used by the failed operation.

#### field PathError.Err

```go
Err error
```

Err is the underlying error.

#### func PathError.Error

```go
func (e *PathError) Error() string
```

Error returns a description of the failed path operation.

#### func PathError.Timeout

```go
func (e *PathError) Timeout() bool
```

Timeout reports whether this error represents a timeout.

#### func PathError.Unwrap

```go
func (e *PathError) Unwrap() error
```

Unwrap returns the underlying error.

### type ReadDirFS

```go
type ReadDirFS interface { ... }
```

ReadDirFS is the interface implemented by a file system that provides an optimized implementation of [ReadDir](fs.md#func-readdir).

#### method ReadDirFS.ReadDir

```go
ReadDir(name string) ([]DirEntry, error)
```

ReadDir reads the named directory and returns a list of directory entries sorted by filename.

### type ReadDirFile

```go
type ReadDirFile interface { ... }
```

A ReadDirFile is a directory file whose entries can be read with the ReadDir method. Every directory file should implement this interface. (It is permissible for any file to implement this interface, but if so ReadDir should return an error for non-directories.)

#### method ReadDirFile.ReadDir

```go
ReadDir(n int) ([]DirEntry, error)
```

ReadDir reads the contents of the directory and returns a slice of up to n DirEntry values in directory order. Subsequent calls on the same file will yield further DirEntry values.

If n > 0, ReadDir returns at most n DirEntry structures. In this case, if ReadDir returns an empty slice, it will return a non-nil error explaining why. At the end of a directory, the error is io.EOF. (ReadDir must return io.EOF itself, not an error wrapping io.EOF.)

If n \<= 0, ReadDir returns all remaining DirEntry values from the directory in a single slice. In this case, if ReadDir succeeds (reads all the way to the end of the directory), it returns the slice and a nil error. If it encounters an error before the end of the directory, ReadDir returns the DirEntry list read until that point and a non-nil error.

### type ReadFileFS

```go
type ReadFileFS interface { ... }
```

ReadFileFS is the interface implemented by a file system that provides an optimized implementation of [ReadFile](fs.md#func-readfile).

#### method ReadFileFS.ReadFile

```go
ReadFile(name string) ([]byte, error)
```

ReadFile reads the named file and returns its contents. A successful call returns a nil error, not io.EOF. (Because ReadFile reads the whole file, the expected EOF from the final Read is not treated as an error to be reported.)

The caller is permitted to modify the returned byte slice. This method should return a copy of the underlying data.

### type ReadLinkFS

```go
type ReadLinkFS interface { ... }
```

ReadLinkFS is the interface implemented by a file system that supports reading symbolic links.

#### method ReadLinkFS.ReadLink

```go
ReadLink(name string) (string, error)
```

ReadLink returns the destination of the named symbolic link. If there is an error, it should be of type [\*PathError](fs.md#type-patherror).

#### method ReadLinkFS.Lstat

```go
Lstat(name string) (FileInfo, error)
```

Lstat returns a [FileInfo](fs.md#type-fileinfo) describing the named file. If the file is a symbolic link, the returned [FileInfo](fs.md#type-fileinfo) describes the symbolic link. Lstat makes no attempt to follow the link. If there is an error, it should be of type [\*PathError](fs.md#type-patherror).

### type StatFS

```go
type StatFS interface { ... }
```

A StatFS is a file system with a Stat method.

#### method StatFS.Stat

```go
Stat(name string) (FileInfo, error)
```

Stat returns a FileInfo describing the file. If there is an error, it should be of type \*PathError.

### type SubFS

```go
type SubFS interface { ... }
```

A SubFS is a file system with a Sub method.

#### method SubFS.Sub

```go
Sub(dir string) (FS, error)
```

Sub returns an FS corresponding to the subtree rooted at dir.

### type WalkDirFunc

```go
type WalkDirFunc func
```

WalkDirFunc is the type of the function called by [WalkDir](fs.md#func-walkdir) to visit each file or directory.

The path argument contains the argument to [WalkDir](fs.md#func-walkdir) as a prefix. That is, if WalkDir is called with root argument "dir" and finds a file named "a" in that directory, the walk function will be called with argument "dir/a".

The d argument is the [DirEntry](fs.md#type-direntry) for the named path.

The error result returned by the function controls how [WalkDir](fs.md#func-walkdir) continues. If the function returns the special value [SkipDir](fs.md#var-skipdir), WalkDir skips the current directory (path if d.IsDir() is true, otherwise path's parent directory). If the function returns the special value [SkipAll](fs.md#var-skipall), WalkDir skips all remaining files and directories. Otherwise, if the function returns a non-nil error, WalkDir stops entirely and returns that error.

The err argument reports an error related to path, signaling that [WalkDir](fs.md#func-walkdir) will not walk into that directory. The function can decide how to handle that error; as described earlier, returning the error will cause WalkDir to stop walking the entire tree.

[WalkDir](fs.md#func-walkdir) calls the function with a non-nil err argument in two cases.

First, if the initial [Stat](fs.md#func-stat) on the root directory fails, WalkDir calls the function with path set to root, d set to nil, and err set to the error from [fs.Stat](fs.md#func-stat).

Second, if a directory's ReadDir method (see [ReadDirFile](fs.md#type-readdirfile)) fails, WalkDir calls the function with path set to the directory's path, d set to an [DirEntry](fs.md#type-direntry) describing the directory, and err set to the error from ReadDir. In this second case, the function is called twice with the path of the directory: the first call is before the directory read is attempted and has err set to nil, giving the function a chance to return [SkipDir](fs.md#var-skipdir) or [SkipAll](fs.md#var-skipall) and avoid the ReadDir entirely. The second call is after a failed ReadDir and reports the error from ReadDir. (If ReadDir succeeds, there is no second call.)

The differences between WalkDirFunc compared to [path/filepath.WalkFunc](../path/filepath.md#type-walkfunc) are:

  - The second argument has type [DirEntry](fs.md#type-direntry) instead of [FileInfo](fs.md#type-fileinfo).
  - The function is called before reading a directory, to allow [SkipDir](fs.md#var-skipdir) or [SkipAll](fs.md#var-skipall) to bypass the directory read entirely or skip all remaining files and directories respectively.
  - If a directory read fails, the function is called a second time for that directory to report the error.
