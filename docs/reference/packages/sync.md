<!-- Generated by mini-go doc. Target: minigo. -->

# Package `sync`

```go
package sync
```

Package sync provides basic synchronization primitives such as mutual exclusion locks. Other than the [Once](sync.md#type-once) and [WaitGroup](sync.md#type-waitgroup) types, most are intended for use by low-level library routines. Higher-level synchronization is better done via channels and communication.

Values containing the types defined in this package should not be copied.

## Index

- [type Cond](#type-cond)
  - [Cond.Broadcast](#func-condbroadcast)
  - [Cond.Signal](#func-condsignal)
  - [Cond.Wait](#func-condwait)
- [type Locker](#type-locker)
- [type Map](#type-map)
  - [Map.Clear](#func-mapclear)
  - [Map.CompareAndDelete](#func-mapcompareanddelete)
  - [Map.CompareAndSwap](#func-mapcompareandswap)
  - [Map.Delete](#func-mapdelete)
  - [Map.Load](#func-mapload)
  - [Map.LoadAndDelete](#func-maploadanddelete)
  - [Map.LoadOrStore](#func-maploadorstore)
  - [Map.Range](#func-maprange)
  - [Map.Store](#func-mapstore)
  - [Map.Swap](#func-mapswap)
- [type Mutex](#type-mutex)
  - [Mutex.Lock](#func-mutexlock)
  - [Mutex.TryLock](#func-mutextrylock)
  - [Mutex.Unlock](#func-mutexunlock)
- [func NewCond](#func-newcond)
- [type Once](#type-once)
  - [Once.Do](#func-oncedo)
- [func OnceFunc](#func-oncefunc)
- [func OnceValue](#func-oncevalue)
- [func OnceValues](#func-oncevalues)
- [type Pool](#type-pool)
  - [Pool.Get](#func-poolget)
  - [Pool.Put](#func-poolput)
- [type RWMutex](#type-rwmutex)
  - [RWMutex.Lock](#func-rwmutexlock)
  - [RWMutex.RLock](#func-rwmutexrlock)
  - [RWMutex.RLocker](#func-rwmutexrlocker)
  - [RWMutex.RUnlock](#func-rwmutexrunlock)
  - [RWMutex.TryLock](#func-rwmutextrylock)
  - [RWMutex.TryRLock](#func-rwmutextryrlock)
  - [RWMutex.Unlock](#func-rwmutexunlock)
- [type WaitGroup](#type-waitgroup)
  - [WaitGroup.Add](#func-waitgroupadd)
  - [WaitGroup.Done](#func-waitgroupdone)
  - [WaitGroup.Go](#func-waitgroupgo)
  - [WaitGroup.Wait](#func-waitgroupwait)

## Functions

### func NewCond

```go
func NewCond(locker Locker) *Cond
```

NewCond returns a new Cond with Locker l.

### func OnceFunc

```go
func OnceFunc(function func()) func()
```

OnceFunc returns a function that invokes f only once. The returned function may be called concurrently.

If f panics, the returned function will panic with the same value on every call.

### func OnceValue

```go
func OnceValue[T any](function func() T) func() T
```

OnceValue returns a function that invokes f only once and returns the value returned by f. The returned function may be called concurrently.

If f panics, the returned function will panic with the same value on every call.

### func OnceValues

```go
func OnceValues[T1, T2 any](function func() (T1, T2)) func() (T1, T2)
```

OnceValues returns a function that invokes f only once and returns the values returned by f. The returned function may be called concurrently.

If f panics, the returned function will panic with the same value on every call.

## Types

### type Cond

```go
type Cond struct { ... }
```

Cond implements a condition variable, a rendezvous point for goroutines waiting for or announcing the occurrence of an event.

Each Cond has an associated Locker L (often a [\*Mutex](sync.md#type-mutex) or [\*RWMutex](sync.md#type-rwmutex)), which must be held when changing the condition and when calling the [Cond.Wait](sync.md#func-condwait) method.

A Cond must not be copied after first use.

In the terminology of [the Go memory model](https://go.dev/ref/mem), Cond arranges that a call to [Cond.Broadcast](sync.md#func-condbroadcast) or [Cond.Signal](sync.md#func-condsignal) “synchronizes before” any Wait call that it unblocks.

For many simple use cases, users will be better off using channels than a Cond (Broadcast corresponds to closing a channel, and Signal corresponds to sending on a channel).

For more on replacements for [sync.Cond](sync.md#type-cond), see [Roberto Clapis's series on advanced concurrency patterns](https://blogtitle.github.io/categories/concurrency/), as well as [Bryan Mills's talk on concurrency patterns](https://drive.google.com/file/d/1nPdvhB0PutEJzdCq5ms6UI58dp50fcAN/view).

#### field Cond.L

```go
L Locker
```

L is held while observing or changing the condition

#### func Cond.Broadcast

```go
func (condition *Cond) Broadcast()
```

Broadcast wakes all goroutines waiting on c.

It is allowed but not required for the caller to hold c.L during the call.

#### func Cond.Signal

```go
func (condition *Cond) Signal()
```

Signal wakes one goroutine waiting on c, if there is any.

It is allowed but not required for the caller to hold c.L during the call.

Signal() does not affect goroutine scheduling priority; if other goroutines are attempting to lock c.L, they may be awoken before a "waiting" goroutine.

#### func Cond.Wait

```go
func (condition *Cond) Wait()
```

Wait atomically unlocks c.L and suspends execution of the calling goroutine. After later resuming execution, Wait locks c.L before returning. Unlike in other systems, Wait cannot return unless awoken by [Cond.Broadcast](sync.md#func-condbroadcast) or [Cond.Signal](sync.md#func-condsignal).

Because c.L is not locked while Wait is waiting, the caller typically cannot assume that the condition is true when Wait returns. Instead, the caller should Wait in a loop:

	c.L.Lock()
	for !condition() {
	    c.Wait()
	}
	... make use of condition ...
	c.L.Unlock()

### type Locker

```go
type Locker interface { ... }
```

A Locker represents an object that can be locked and unlocked.

#### method Locker.Lock

```go
Lock()
```

Lock acquires the lock, blocking until it is available.

#### method Locker.Unlock

```go
Unlock()
```

Unlock releases the lock.

### type Map

```go
type Map struct { ... }
```

Map is like a Go map\[any]any but is safe for concurrent use by multiple goroutines without additional locking or coordination. Loads, stores, and deletes run in amortized constant time.

The Map type is specialized. Most code should use a plain Go map instead, with separate locking or coordination, for better type safety and to make it easier to maintain other invariants along with the map content.

The Map type is optimized for two common use cases: (1) when the entry for a given key is only ever written once but read many times, as in caches that only grow, or (2) when multiple goroutines read, write, and overwrite entries for disjoint sets of keys. In these two cases, use of a Map may significantly reduce lock contention compared to a Go map paired with a separate [Mutex](sync.md#type-mutex) or [RWMutex](sync.md#type-rwmutex).

The zero Map is empty and ready for use. A Map must not be copied after first use.

In the terminology of [the Go memory model](https://go.dev/ref/mem), Map arranges that a write operation “synchronizes before” any read operation that observes the effect of the write, where read and write operations are defined as follows. [Map.Load](sync.md#func-mapload), [Map.LoadAndDelete](sync.md#func-maploadanddelete), [Map.LoadOrStore](sync.md#func-maploadorstore), [Map.Swap](sync.md#func-mapswap), [Map.CompareAndSwap](sync.md#func-mapcompareandswap), and [Map.CompareAndDelete](sync.md#func-mapcompareanddelete) are read operations; [Map.Delete](sync.md#func-mapdelete), [Map.LoadAndDelete](sync.md#func-maploadanddelete), [Map.Store](sync.md#func-mapstore), and [Map.Swap](sync.md#func-mapswap) are write operations; [Map.LoadOrStore](sync.md#func-maploadorstore) is a write operation when it returns loaded set to false; [Map.CompareAndSwap](sync.md#func-mapcompareandswap) is a write operation when it returns swapped set to true; and [Map.CompareAndDelete](sync.md#func-mapcompareanddelete) is a write operation when it returns deleted set to true.

#### func Map.Clear

```go
func (table *Map) Clear()
```

Clear deletes all the entries, resulting in an empty Map.

#### func Map.CompareAndDelete

```go
func (table *Map) CompareAndDelete(key, old any) bool
```

CompareAndDelete deletes the entry for key if its value is equal to old. The old value must be of a comparable type.

If there is no current value for key in the map, CompareAndDelete returns false (even if the old value is the nil interface value).

#### func Map.CompareAndSwap

```go
func (table *Map) CompareAndSwap(key, old, new any) bool
```

CompareAndSwap swaps the old and new values for key if the value stored in the map is equal to old. The old value must be of a comparable type.

#### func Map.Delete

```go
func (table *Map) Delete(key any)
```

Delete deletes the value for a key. If the key is not in the map, Delete does nothing.

#### func Map.Load

```go
func (table *Map) Load(key any) (any, bool)
```

Load returns the value stored in the map for a key, or nil if no value is present. The ok result indicates whether value was found in the map.

#### func Map.LoadAndDelete

```go
func (table *Map) LoadAndDelete(key any) (any, bool)
```

LoadAndDelete deletes the value for a key, returning the previous value if any. The loaded result reports whether the key was present.

#### func Map.LoadOrStore

```go
func (table *Map) LoadOrStore(key, value any) (any, bool)
```

LoadOrStore returns the existing value for the key if present. Otherwise, it stores and returns the given value. The loaded result is true if the value was loaded, false if stored.

#### func Map.Range

```go
func (table *Map) Range(function func(key, value any) bool)
```

Range calls f sequentially for each key and value present in the map. If f returns false, range stops the iteration.

Range does not necessarily correspond to any consistent snapshot of the Map's contents: no key will be visited more than once, but if the value for any key is stored or deleted concurrently (including by f), Range may reflect any mapping for that key from any point during the Range call. Range does not block other methods on the receiver; even f itself may call any method on m.

Range may be O(N) with the number of elements in the map even if f returns false after a constant number of calls.

#### func Map.Store

```go
func (table *Map) Store(key, value any)
```

Store sets the value for a key.

#### func Map.Swap

```go
func (table *Map) Swap(key, value any) (any, bool)
```

Swap swaps the value for a key and returns the previous value if any. The loaded result reports whether the key was present.

### type Mutex

```go
type Mutex struct { ... }
```

A Mutex is a mutual exclusion lock. The zero value for a Mutex is an unlocked mutex.

A Mutex must not be copied after first use.

In the terminology of [the Go memory model](https://go.dev/ref/mem), the n'th call to [Mutex.Unlock](sync.md#func-mutexunlock) “synchronizes before” the m'th call to [Mutex.Lock](sync.md#func-mutexlock) for any n \< m. A successful call to [Mutex.TryLock](sync.md#func-mutextrylock) is equivalent to a call to Lock. A failed call to TryLock does not establish any “synchronizes before” relation at all.

#### func Mutex.Lock

```go
func (mutex *Mutex) Lock()
```

Lock locks m. If the lock is already in use, the calling goroutine blocks until the mutex is available.

#### func Mutex.TryLock

```go
func (mutex *Mutex) TryLock() bool
```

TryLock tries to lock m and reports whether it succeeded.

Note that while correct uses of TryLock do exist, they are rare, and use of TryLock is often a sign of a deeper problem in a particular use of mutexes.

#### func Mutex.Unlock

```go
func (mutex *Mutex) Unlock()
```

Unlock unlocks m. It is a run-time error if m is not locked on entry to Unlock.

A locked [Mutex](sync.md#type-mutex) is not associated with a particular goroutine. It is allowed for one goroutine to lock a Mutex and then arrange for another goroutine to unlock it.

### type Once

```go
type Once struct { ... }
```

Once is an object that will perform exactly one action.

A Once must not be copied after first use.

In the terminology of [the Go memory model](https://go.dev/ref/mem), the return from f “synchronizes before” the return from any call of once.Do(f).

#### func Once.Do

```go
func (once *Once) Do(function func())
```

Do calls the function f if and only if Do is being called for the first time for this instance of [Once](sync.md#type-once). In other words, given

	var once Once

if once.Do(f) is called multiple times, only the first call will invoke f, even if f has a different value in each invocation. A new instance of Once is required for each function to execute.

Do is intended for initialization that must be run exactly once. Since f is niladic, it may be necessary to use a function literal to capture the arguments to a function to be invoked by Do:

	config.once.Do(func() { config.init(filename) })

Because no call to Do returns until the one call to f returns, if f causes Do to be called, it will deadlock.

If f panics, Do considers it to have returned; future calls of Do return without calling f.

### type Pool

```go
type Pool struct { ... }
```

A Pool is a set of temporary objects that may be individually saved and retrieved.

Any item stored in the Pool may be removed automatically at any time without notification. If the Pool holds the only reference when this happens, the item might be deallocated.

A Pool is safe for use by multiple goroutines simultaneously.

Pool's purpose is to cache allocated but unused items for later reuse, relieving pressure on the garbage collector. That is, it makes it easy to build efficient, thread-safe free lists. However, it is not suitable for all free lists.

An appropriate use of a Pool is to manage a group of temporary items silently shared among and potentially reused by concurrent independent clients of a package. Pool provides a way to amortize allocation overhead across many clients.

An example of good use of a Pool is in the fmt package, which maintains a dynamically-sized store of temporary output buffers. The store scales under load (when many goroutines are actively printing) and shrinks when quiescent.

On the other hand, a free list maintained as part of a short-lived object is not a suitable use for a Pool, since the overhead does not amortize well in that scenario. It is more efficient to have such objects implement their own free list.

A Pool must not be copied after first use.

In the terminology of [the Go memory model](https://go.dev/ref/mem), a call to Put(x) “synchronizes before” a call to [Pool.Get](sync.md#func-poolget) returning that same value x. Similarly, a call to New returning x “synchronizes before” a call to Get returning that same value x.

#### field Pool.New

```go
New func() any
```

New optionally specifies a function to generate a value when Get would otherwise return nil. It may not be changed concurrently with calls to Get.

#### func Pool.Get

```go
func (pool *Pool) Get() any
```

Get selects an arbitrary item from the [Pool](sync.md#type-pool), removes it from the Pool, and returns it to the caller. Get may choose to ignore the pool and treat it as empty. Callers should not assume any relation between values passed to [Pool.Put](sync.md#func-poolput) and the values returned by Get.

If Get would otherwise return nil and p.New is non-nil, Get returns the result of calling p.New.

#### func Pool.Put

```go
func (pool *Pool) Put(value any)
```

Put adds x to the pool.

### type RWMutex

```go
type RWMutex struct { ... }
```

A RWMutex is a reader/writer mutual exclusion lock. The lock can be held by an arbitrary number of readers or a single writer. The zero value for a RWMutex is an unlocked mutex.

A RWMutex must not be copied after first use.

If any goroutine calls [RWMutex.Lock](sync.md#func-rwmutexlock) while the lock is already held by one or more readers, concurrent calls to [RWMutex.RLock](sync.md#func-rwmutexrlock) will block until the writer has acquired (and released) the lock, to ensure that the lock eventually becomes available to the writer. Note that this prohibits recursive read-locking. A [RWMutex.RLock](sync.md#func-rwmutexrlock) cannot be upgraded into a [RWMutex.Lock](sync.md#func-rwmutexlock), nor can a [RWMutex.Lock](sync.md#func-rwmutexlock) be downgraded into a [RWMutex.RLock](sync.md#func-rwmutexrlock).

In the terminology of [the Go memory model](https://go.dev/ref/mem), the n'th call to [RWMutex.Unlock](sync.md#func-rwmutexunlock) “synchronizes before” the m'th call to Lock for any n \< m, just as for [Mutex](sync.md#type-mutex). For any call to RLock, there exists an n such that the n'th call to Unlock “synchronizes before” that call to RLock, and the corresponding call to [RWMutex.RUnlock](sync.md#func-rwmutexrunlock) “synchronizes before” the n+1'th call to Lock.

#### func RWMutex.Lock

```go
func (mutex *RWMutex) Lock()
```

Lock locks rw for writing. If the lock is already locked for reading or writing, Lock blocks until the lock is available.

#### func RWMutex.RLock

```go
func (mutex *RWMutex) RLock()
```

RLock locks rw for reading.

It should not be used for recursive read locking; a blocked Lock call excludes new readers from acquiring the lock. See the documentation on the [RWMutex](sync.md#type-rwmutex) type.

#### func RWMutex.RLocker

```go
func (mutex *RWMutex) RLocker() Locker
```

RLocker returns a [Locker](sync.md#type-locker) interface that implements the \[Locker.Lock] and \[Locker.Unlock] methods by calling rw.RLock and rw.RUnlock.

#### func RWMutex.RUnlock

```go
func (mutex *RWMutex) RUnlock()
```

RUnlock undoes a single [RWMutex.RLock](sync.md#func-rwmutexrlock) call; it does not affect other simultaneous readers. It is a run-time error if rw is not locked for reading on entry to RUnlock.

#### func RWMutex.TryLock

```go
func (mutex *RWMutex) TryLock() bool
```

TryLock tries to lock rw for writing and reports whether it succeeded.

Note that while correct uses of TryLock do exist, they are rare, and use of TryLock is often a sign of a deeper problem in a particular use of mutexes.

#### func RWMutex.TryRLock

```go
func (mutex *RWMutex) TryRLock() bool
```

TryRLock tries to lock rw for reading and reports whether it succeeded.

Note that while correct uses of TryRLock do exist, they are rare, and use of TryRLock is often a sign of a deeper problem in a particular use of mutexes.

#### func RWMutex.Unlock

```go
func (mutex *RWMutex) Unlock()
```

Unlock unlocks rw for writing. It is a run-time error if rw is not locked for writing on entry to Unlock.

As with Mutexes, a locked [RWMutex](sync.md#type-rwmutex) is not associated with a particular goroutine. One goroutine may [RWMutex.RLock](sync.md#func-rwmutexrlock) ([RWMutex.Lock](sync.md#func-rwmutexlock)) a RWMutex and then arrange for another goroutine to [RWMutex.RUnlock](sync.md#func-rwmutexrunlock) ([RWMutex.Unlock](sync.md#func-rwmutexunlock)) it.

### type WaitGroup

```go
type WaitGroup struct { ... }
```

A WaitGroup is a counting semaphore typically used to wait for a group of goroutines or tasks to finish.

Typically, a main goroutine will start tasks, each in a new goroutine, by calling [WaitGroup.Go](sync.md#func-waitgroupgo) and then wait for all tasks to complete by calling [WaitGroup.Wait](sync.md#func-waitgroupwait). For example:

	var wg sync.WaitGroup
	wg.Go(task1)
	wg.Go(task2)
	wg.Wait()

A WaitGroup may also be used for tracking tasks without using Go to start new goroutines by using [WaitGroup.Add](sync.md#func-waitgroupadd) and [WaitGroup.Done](sync.md#func-waitgroupdone).

The previous example can be rewritten using explicitly created goroutines along with Add and Done:

	var wg sync.WaitGroup
	wg.Add(1)
	go func() {
		defer wg.Done()
		task1()
	}()
	wg.Add(1)
	go func() {
		defer wg.Done()
		task2()
	}()
	wg.Wait()

This pattern is common in code that predates [WaitGroup.Go](sync.md#func-waitgroupgo).

A WaitGroup must not be copied after first use.

#### func WaitGroup.Add

```go
func (group *WaitGroup) Add(delta int)
```

Add adds delta, which may be negative, to the [WaitGroup](sync.md#type-waitgroup) task counter. If the counter becomes zero, all goroutines blocked on [WaitGroup.Wait](sync.md#func-waitgroupwait) are released. If the counter goes negative, Add panics.

Callers should prefer [WaitGroup.Go](sync.md#func-waitgroupgo).

Note that calls with a positive delta that occur when the counter is zero must happen before a Wait. Calls with a negative delta, or calls with a positive delta that start when the counter is greater than zero, may happen at any time. Typically this means the calls to Add should execute before the statement creating the goroutine or other event to be waited for. If a WaitGroup is reused to wait for several independent sets of events, new Add calls must happen after all previous Wait calls have returned. See the WaitGroup example.

#### func WaitGroup.Done

```go
func (group *WaitGroup) Done()
```

Done decrements the [WaitGroup](sync.md#type-waitgroup) task counter by one. It is equivalent to Add(-1).

Callers should prefer [WaitGroup.Go](sync.md#func-waitgroupgo).

In the terminology of [the Go memory model](https://go.dev/ref/mem), a call to Done "synchronizes before" the return of any Wait call that it unblocks.

#### func WaitGroup.Go

```go
func (group *WaitGroup) Go(function func())
```

Go calls f in a new goroutine and adds that task to the [WaitGroup](sync.md#type-waitgroup). When f returns, the task is removed from the WaitGroup.

The function f must not panic.

If the WaitGroup is empty, Go must happen before a [WaitGroup.Wait](sync.md#func-waitgroupwait). Typically, this simply means Go is called to start tasks before Wait is called. If the WaitGroup is not empty, Go may happen at any time. This means a goroutine started by Go may itself call Go. If a WaitGroup is reused to wait for several independent sets of tasks, new Go calls must happen after all previous Wait calls have returned.

In the terminology of [the Go memory model](https://go.dev/ref/mem), the return from f "synchronizes before" the return of any Wait call that it unblocks.

#### func WaitGroup.Wait

```go
func (group *WaitGroup) Wait()
```

Wait blocks until the [WaitGroup](sync.md#type-waitgroup) task counter is zero.
