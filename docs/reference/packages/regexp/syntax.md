<!-- Generated by mini-go doc. Target: minigo. -->

# Package `regexp/syntax`

```go
package syntax
```

Package syntax parses regular expressions into parse trees and compiles parse trees into programs. Most clients of regular expressions will use the facilities of package [regexp](../regexp.md) (such as [regexp.Compile](../regexp.md#func-compile) and [regexp.Match](../regexp.md#func-match)) instead of this package.

#### Syntax

The regular expression syntax understood by this package when parsing with the [Perl](syntax.md#const-perl) flag is as follows. Parts of the syntax can be disabled by passing alternate flags to [Parse](syntax.md#func-parse).

Single characters:

	.              any character, possibly including newline (flag s=true)
	[xyz]          character class
	[^xyz]         negated character class
	\d             Perl character class
	\D             negated Perl character class
	[[:alpha:]]    ASCII character class
	[[:^alpha:]]   negated ASCII character class
	\pN            Unicode character class (one-letter name)
	\p{Greek}      Unicode character class
	\PN            negated Unicode character class (one-letter name)
	\P{Greek}      negated Unicode character class

Composites:

	xy             x followed by y
	x|y            x or y (prefer x)

Repetitions:

	x*             zero or more x, prefer more
	x+             one or more x, prefer more
	x?             zero or one x, prefer one
	x{n,m}         n or n+1 or ... or m x, prefer more
	x{n,}          n or more x, prefer more
	x{n}           exactly n x
	x*?            zero or more x, prefer fewer
	x+?            one or more x, prefer fewer
	x??            zero or one x, prefer zero
	x{n,m}?        n or n+1 or ... or m x, prefer fewer
	x{n,}?         n or more x, prefer fewer
	x{n}?          exactly n x

Implementation restriction: The counting forms x{n,m}, x{n,}, and x{n} reject forms that create a minimum or maximum repetition count above 1000. Unlimited repetitions are not subject to this restriction.

Grouping:

	(re)           numbered capturing group (submatch)
	(?P<name>re)   named & numbered capturing group (submatch)
	(?<name>re)    named & numbered capturing group (submatch)
	(?:re)         non-capturing group
	(?flags)       set flags within current group; non-capturing
	(?flags:re)    set flags during re; non-capturing

	Flag syntax is xyz (set) or -xyz (clear) or xy-z (set xy, clear z). The flags are:

	i              case-insensitive (default false)
	m              multi-line mode: ^ and $ match begin/end line in addition to begin/end text (default false)
	s              let . match \n (default false)
	U              ungreedy: swap meaning of x* and x*?, x+ and x+?, etc (default false)

Empty strings:

	^              at beginning of text or line (flag m=true)
	$              at end of text (like \z not \Z) or line (flag m=true)
	\A             at beginning of text
	\b             at ASCII word boundary (\w on one side and \W, \A, or \z on the other)
	\B             not at ASCII word boundary
	\z             at end of text

Escape sequences:

	\a             bell (== \007)
	\f             form feed (== \014)
	\t             horizontal tab (== \011)
	\n             newline (== \012)
	\r             carriage return (== \015)
	\v             vertical tab character (== \013)
	\*             literal *, for any punctuation character *
	\123           octal character code (up to three digits)
	\x7F           hex character code (exactly two digits)
	\x{10FFFF}     hex character code
	\Q...\E        literal text ... even if ... has punctuation

Character class elements:

	x              single character
	A-Z            character range (inclusive)
	\d             Perl character class
	[:foo:]        ASCII character class foo
	\p{Foo}        Unicode character class Foo
	\pF            Unicode character class F (one-letter name)

Named character classes as character class elements:

	[\d]           digits (== \d)
	[^\d]          not digits (== \D)
	[\D]           not digits (== \D)
	[^\D]          not not digits (== \d)
	[[:name:]]     named ASCII class inside character class (== [:name:])
	[^[:name:]]    named ASCII class inside negated character class (== [:^name:])
	[\p{Name}]     named Unicode property inside character class (== \p{Name})
	[^\p{Name}]    named Unicode property inside negated character class (== \P{Name})

Perl character classes (all ASCII-only):

	\d             digits (== [0-9])
	\D             not digits (== [^0-9])
	\s             whitespace (== [\t\n\f\r ])
	\S             not whitespace (== [^\t\n\f\r ])
	\w             word characters (== [0-9A-Za-z_])
	\W             not word characters (== [^0-9A-Za-z_])

ASCII character classes:

	[[:alnum:]]    alphanumeric (== [0-9A-Za-z])
	[[:alpha:]]    alphabetic (== [A-Za-z])
	[[:ascii:]]    ASCII (== [\x00-\x7F])
	[[:blank:]]    blank (== [\t ])
	[[:cntrl:]]    control (== [\x00-\x1F\x7F])
	[[:digit:]]    digits (== [0-9])
	[[:graph:]]    graphical (== [!-~] == [A-Za-z0-9!"#$%&'()*+,\-./:;<=>?@[\\\]^_`{|}~])
	[[:lower:]]    lower case (== [a-z])
	[[:print:]]    printable (== [ -~] == [ [:graph:]])
	[[:punct:]]    punctuation (== [!-/:-@[-`{-~])
	[[:space:]]    whitespace (== [\t\n\v\f\r ])
	[[:upper:]]    upper case (== [A-Z])
	[[:word:]]     word characters (== [0-9A-Za-z_])
	[[:xdigit:]]   hex digit (== [0-9A-Fa-f])

Unicode character classes are those in [unicode.Categories](../unicode.md#var-categories), [unicode.CategoryAliases](../unicode.md#var-categoryaliases), and [unicode.Scripts](../unicode.md#var-scripts).

## Index

- [const ClassNL](#const-classnl)
- [func Compile](#func-compile)
- [const DotNL](#const-dotnl)
- [const EmptyBeginLine](#const-emptybeginline)
- [const EmptyBeginText](#const-emptybegintext)
- [const EmptyEndLine](#const-emptyendline)
- [const EmptyEndText](#const-emptyendtext)
- [const EmptyNoWordBoundary](#const-emptynowordboundary)
- [type EmptyOp](#type-emptyop)
- [func EmptyOpContext](#func-emptyopcontext)
- [const EmptyWordBoundary](#const-emptywordboundary)
- [const ErrInternalError](#const-errinternalerror)
- [const ErrInvalidCharClass](#const-errinvalidcharclass)
- [const ErrInvalidCharRange](#const-errinvalidcharrange)
- [const ErrInvalidEscape](#const-errinvalidescape)
- [const ErrInvalidNamedCapture](#const-errinvalidnamedcapture)
- [const ErrInvalidPerlOp](#const-errinvalidperlop)
- [const ErrInvalidRepeatOp](#const-errinvalidrepeatop)
- [const ErrInvalidRepeatSize](#const-errinvalidrepeatsize)
- [const ErrInvalidUTF8](#const-errinvalidutf8)
- [const ErrLarge](#const-errlarge)
- [const ErrMissingBracket](#const-errmissingbracket)
- [const ErrMissingParen](#const-errmissingparen)
- [const ErrMissingRepeatArgument](#const-errmissingrepeatargument)
- [const ErrNestingDepth](#const-errnestingdepth)
- [const ErrTrailingBackslash](#const-errtrailingbackslash)
- [const ErrUnexpectedParen](#const-errunexpectedparen)
- [type Error](#type-error)
  - [Error.Error](#func-errorerror)
- [type ErrorCode](#type-errorcode)
  - [ErrorCode.String](#func-errorcodestring)
- [type Flags](#type-flags)
- [const FoldCase](#const-foldcase)
- [type Inst](#type-inst)
  - [Inst.MatchEmptyWidth](#func-instmatchemptywidth)
  - [Inst.MatchRune](#func-instmatchrune)
  - [Inst.MatchRunePos](#func-instmatchrunepos)
  - [Inst.String](#func-inststring)
- [const InstAlt](#const-instalt)
- [const InstAltMatch](#const-instaltmatch)
- [const InstCapture](#const-instcapture)
- [const InstEmptyWidth](#const-instemptywidth)
- [const InstFail](#const-instfail)
- [const InstMatch](#const-instmatch)
- [const InstNop](#const-instnop)
- [type InstOp](#type-instop)
  - [InstOp.String](#func-instopstring)
- [const InstRune](#const-instrune)
- [const InstRune1](#const-instrune1)
- [const InstRuneAny](#const-instruneany)
- [const InstRuneAnyNotNL](#const-instruneanynotnl)
- [func IsWordChar](#func-iswordchar)
- [const Literal](#const-literal)
- [const MatchNL](#const-matchnl)
- [const NonGreedy](#const-nongreedy)
- [const OneLine](#const-oneline)
- [type Op](#type-op)
  - [Op.String](#func-opstring)
- [const OpAlternate](#const-opalternate)
- [const OpAnyChar](#const-opanychar)
- [const OpAnyCharNotNL](#const-opanycharnotnl)
- [const OpBeginLine](#const-opbeginline)
- [const OpBeginText](#const-opbegintext)
- [const OpCapture](#const-opcapture)
- [const OpCharClass](#const-opcharclass)
- [const OpConcat](#const-opconcat)
- [const OpEmptyMatch](#const-opemptymatch)
- [const OpEndLine](#const-opendline)
- [const OpEndText](#const-opendtext)
- [const OpLiteral](#const-opliteral)
- [const OpNoMatch](#const-opnomatch)
- [const OpNoWordBoundary](#const-opnowordboundary)
- [const OpPlus](#const-opplus)
- [const OpQuest](#const-opquest)
- [const OpRepeat](#const-oprepeat)
- [const OpStar](#const-opstar)
- [const OpWordBoundary](#const-opwordboundary)
- [const POSIX](#const-posix)
- [func Parse](#func-parse)
- [const Perl](#const-perl)
- [const PerlX](#const-perlx)
- [type Prog](#type-prog)
  - [Prog.Prefix](#func-progprefix)
  - [Prog.StartCond](#func-progstartcond)
  - [Prog.String](#func-progstring)
- [type Regexp](#type-regexp)
  - [Regexp.CapNames](#func-regexpcapnames)
  - [Regexp.Equal](#func-regexpequal)
  - [Regexp.MaxCap](#func-regexpmaxcap)
  - [Regexp.Simplify](#func-regexpsimplify)
  - [Regexp.String](#func-regexpstring)
- [const Simple](#const-simple)
- [const UnicodeGroups](#const-unicodegroups)
- [const WasDollar](#const-wasdollar)

## Constants

### const ClassNL

```go

```

treat pattern as literal string

### const DotNL

```go

```

allow character classes like \[^a-z] and \[\[:space:]] to match newline

### const EmptyBeginLine

```go
const EmptyBeginLine EmptyOp = 1 << iota
```

EmptyBeginLine matches the beginning of a line.

### const EmptyBeginText

```go

```

EmptyBeginText matches the beginning of text.

### const EmptyEndLine

```go

```

EmptyEndLine matches the end of a line.

### const EmptyEndText

```go

```

EmptyEndText matches the end of text.

### const EmptyNoWordBoundary

```go

```

EmptyNoWordBoundary matches a position that is not a word boundary.

### const EmptyWordBoundary

```go

```

EmptyWordBoundary matches a word boundary.

### const ErrInternalError

```go
const ErrInternalError ErrorCode = "regexp/syntax: internal error"
```

Unexpected error

### const ErrInvalidCharClass

```go
const ErrInvalidCharClass ErrorCode = "invalid character class"
```

Parse errors

### const ErrInvalidCharRange

```go
const ErrInvalidCharRange ErrorCode = "invalid character class range"
```

ErrInvalidCharRange indicates an invalid character class range.

### const ErrInvalidEscape

```go
const ErrInvalidEscape ErrorCode = "invalid escape sequence"
```

ErrInvalidEscape indicates an invalid escape sequence.

### const ErrInvalidNamedCapture

```go
const ErrInvalidNamedCapture ErrorCode = "invalid named capture"
```

ErrInvalidNamedCapture indicates an invalid named capture.

### const ErrInvalidPerlOp

```go
const ErrInvalidPerlOp ErrorCode = "invalid or unsupported Perl syntax"
```

ErrInvalidPerlOp indicates invalid or unsupported Perl syntax.

### const ErrInvalidRepeatOp

```go
const ErrInvalidRepeatOp ErrorCode = "invalid nested repetition operator"
```

ErrInvalidRepeatOp indicates a nested repetition operator.

### const ErrInvalidRepeatSize

```go
const ErrInvalidRepeatSize ErrorCode = "invalid repeat count"
```

ErrInvalidRepeatSize indicates an invalid repeat count.

### const ErrInvalidUTF8

```go
const ErrInvalidUTF8 ErrorCode = "invalid UTF-8"
```

ErrInvalidUTF8 indicates invalid UTF-8 text.

### const ErrLarge

```go
const ErrLarge ErrorCode = "expression too large"
```

ErrLarge indicates that an expression is too large.

### const ErrMissingBracket

```go
const ErrMissingBracket ErrorCode = "missing closing ]"
```

ErrMissingBracket indicates a missing closing bracket.

### const ErrMissingParen

```go
const ErrMissingParen ErrorCode = "missing closing )"
```

ErrMissingParen indicates a missing closing parenthesis.

### const ErrMissingRepeatArgument

```go
const ErrMissingRepeatArgument ErrorCode = "missing argument to repetition operator"
```

ErrMissingRepeatArgument indicates a repetition operator without an argument.

### const ErrNestingDepth

```go
const ErrNestingDepth ErrorCode = "expression nests too deeply"
```

ErrNestingDepth indicates excessive expression nesting.

### const ErrTrailingBackslash

```go
const ErrTrailingBackslash ErrorCode = "trailing backslash at end of expression"
```

ErrTrailingBackslash indicates a trailing backslash.

### const ErrUnexpectedParen

```go
const ErrUnexpectedParen ErrorCode = "unexpected )"
```

ErrUnexpectedParen indicates an unexpected closing parenthesis.

### const FoldCase

```go
const FoldCase Flags = 1 << iota
```

FoldCase enables case-insensitive matching.

### const InstAlt

```go
const InstAlt InstOp = iota
```

InstAlt chooses between Out and Arg.

### const InstAltMatch

```go

```

InstAltMatch is an alternate instruction that prefers a match.

### const InstCapture

```go

```

InstCapture records a capture boundary.

### const InstEmptyWidth

```go

```

InstEmptyWidth matches an empty-width condition.

### const InstFail

```go

```

InstFail always fails.

### const InstMatch

```go

```

InstMatch marks a successful match.

### const InstNop

```go

```

InstNop proceeds to the next instruction.

### const InstRune

```go

```

InstRune matches a rune range.

### const InstRune1

```go

```

InstRune1 matches one rune.

### const InstRuneAny

```go

```

InstRuneAny matches any rune.

### const InstRuneAnyNotNL

```go

```

InstRuneAnyNotNL matches any rune except newline.

### const Literal

```go

```

case-insensitive match

### const MatchNL

```go
const MatchNL = ClassNL | DotNL
```

MatchNL enables matching newlines in character classes and dot expressions.

### const NonGreedy

```go

```

treat ^ and $ as only matching at beginning and end of text

### const OneLine

```go

```

allow . to match newline

### const OpAlternate

```go

```

matches concatenation of Subs

### const OpAnyChar

```go

```

matches any character except newline

### const OpAnyCharNotNL

```go

```

matches Runes interpreted as range pair list

### const OpBeginLine

```go

```

matches any character

### const OpBeginText

```go

```

matches empty string at end of line

### const OpCapture

```go

```

matches word non-boundary \`\\B\`

### const OpCharClass

```go

```

matches Runes sequence

### const OpConcat

```go

```

matches Sub\[0] at least Min times, at most Max (Max == -1 is no limit)

### const OpEmptyMatch

```go

```

matches no strings

### const OpEndLine

```go

```

matches empty string at beginning of line

### const OpEndText

```go

```

matches empty string at beginning of text

### const OpLiteral

```go

```

matches empty string

### const OpNoMatch

```go
const OpNoMatch Op = 1 + iota
```

OpNoMatch matches no strings.

### const OpNoWordBoundary

```go

```

matches word boundary \`\\b\`

### const OpPlus

```go

```

matches Sub\[0] zero or more times

### const OpQuest

```go

```

matches Sub\[0] one or more times

### const OpRepeat

```go

```

matches Sub\[0] zero or one times

### const OpStar

```go

```

capturing subexpression with index Cap, optional name Name

### const OpWordBoundary

```go

```

matches empty string at end of text

### const POSIX

```go
const POSIX Flags = 0
```

as close to Perl as possible

### const Perl

```go
const Perl = ClassNL | OneLine | PerlX | UnicodeGroups
```

Perl enables the parser features closest to Perl syntax.

### const PerlX

```go

```

make repetition operators default to non-greedy

### const Simple

```go

```

regexp OpEndText was $, not \\z

### const UnicodeGroups

```go

```

allow Perl extensions

### const WasDollar

```go

```

allow \\p{Han}, \\P{Han} for Unicode group and negation

## Functions

### func Compile

```go
func Compile(re *Regexp) (*Prog, error)
```

Compile compiles the regexp into a program to be executed. The regexp should have been simplified already (returned from re.Simplify).

### func EmptyOpContext

```go
func EmptyOpContext(r1, r2 rune) EmptyOp
```

EmptyOpContext returns the zero-width assertions satisfied at the position between the runes r1 and r2. Passing r1 == -1 indicates that the position is at the beginning of the text. Passing r2 == -1 indicates that the position is at the end of the text.

### func IsWordChar

```go
func IsWordChar(r rune) bool
```

IsWordChar reports whether r is considered a “word character” during the evaluation of the \\b and \\B zero-width assertions. These assertions are ASCII-only: the word characters are \[A-Za-z0-9\_].

### func Parse

```go
func Parse(s string, flags Flags) (*Regexp, error)
```

Parse parses a regular expression string s, controlled by the specified Flags, and returns a regular expression parse tree. The syntax is described in the top-level comment.

## Types

### type EmptyOp

```go
type EmptyOp uint8
```

An EmptyOp specifies a kind or mixture of zero-width assertions.

### type Error

```go
type Error struct { ... }
```

An Error describes a failure to parse a regular expression and gives the offending expression.

#### field Error.Code

```go
Code ErrorCode
```

error code

#### field Error.Expr

```go
Expr string
```

offending expression

#### func Error.Error

```go
func (e *Error) Error() string
```

Error returns a formatted parse error.

### type ErrorCode

```go
type ErrorCode string
```

An ErrorCode describes a failure to parse a regular expression.

#### func ErrorCode.String

```go
func (e ErrorCode) String() string
```

String returns the error code text.

### type Flags

```go
type Flags uint16
```

Flags control the behavior of the parser and record information about regexp context.

### type Inst

```go
type Inst struct { ... }
```

An Inst is a single instruction in a regular expression program.

#### field Inst.Op

```go
Op InstOp
```

instruction opcode

#### field Inst.Out

```go
Out uint32
```

all but InstMatch, InstFail

#### field Inst.Arg

```go
Arg uint32
```

InstAlt, InstAltMatch, InstCapture, InstEmptyWidth

#### field Inst.Rune

```go
Rune []rune
```

InstAlt, InstAltMatch, InstCapture, InstEmptyWidth

#### func Inst.MatchEmptyWidth

```go
func (i *Inst) MatchEmptyWidth(before rune, after rune) bool
```

MatchEmptyWidth reports whether the instruction matches an empty string between the runes before and after. It should only be called when i.Op == [InstEmptyWidth](syntax.md#const-instemptywidth).

#### func Inst.MatchRune

```go
func (i *Inst) MatchRune(r rune) bool
```

MatchRune reports whether the instruction matches (and consumes) r. It should only be called when i.Op == [InstRune](syntax.md#const-instrune).

#### func Inst.MatchRunePos

```go
func (i *Inst) MatchRunePos(r rune) int
```

MatchRunePos checks whether the instruction matches (and consumes) r. If so, MatchRunePos returns the index of the matching rune pair (or, when len(i.Rune) == 1, rune singleton). If not, MatchRunePos returns -1. MatchRunePos should only be called when i.Op == [InstRune](syntax.md#const-instrune).

#### func Inst.String

```go
func (i *Inst) String() string
```

String returns a textual representation of the instruction.

### type InstOp

```go
type InstOp uint8
```

An InstOp is an instruction opcode.

#### func InstOp.String

```go
func (i InstOp) String() string
```

String returns the instruction opcode name.

### type Op

```go
type Op uint8
```

An Op is a single regular expression operator.

#### func Op.String

```go
func (i Op) String() string
```

String returns the operator name.

### type Prog

```go
type Prog struct { ... }
```

A Prog is a compiled regular expression program.

#### field Prog.Inst

```go
Inst []Inst
```

program instructions

#### field Prog.Start

```go
Start int
```

index of start instruction

#### field Prog.NumCap

```go
NumCap int
```

number of InstCapture insts in re

#### func Prog.Prefix

```go
func (p *Prog) Prefix() (prefix string, complete bool)
```

Prefix returns a literal string that all matches for the regexp must start with. Complete is true if the prefix is the entire match.

#### func Prog.StartCond

```go
func (p *Prog) StartCond() EmptyOp
```

StartCond returns the leading empty-width conditions that must be true in any match. It returns ^EmptyOp(0) if no matches are possible.

#### func Prog.String

```go
func (p *Prog) String() string
```

String returns a textual representation of the program.

### type Regexp

```go
type Regexp struct { ... }
```

A Regexp is a node in a regular expression syntax tree.

#### field Regexp.Op

```go
Op Op
```

operator

#### field Regexp.Flags

```go
Flags Flags
```

operator

#### field Regexp.Sub

```go
Sub []*Regexp
```

subexpressions, if any

#### field Regexp.Sub0

```go
Sub0 [1]*Regexp
```

storage for short Sub

#### field Regexp.Rune

```go
Rune []rune
```

matched runes, for OpLiteral, OpCharClass

#### field Regexp.Rune0

```go
Rune0 [2]rune
```

storage for short Rune

#### field Regexp.Min

```go
Min, Max int
```

min, max for OpRepeat

#### field Regexp.Max

```go
Min, Max int
```

min, max for OpRepeat

#### field Regexp.Cap

```go
Cap int
```

capturing index, for OpCapture

#### field Regexp.Name

```go
Name string
```

capturing name, for OpCapture

#### func Regexp.CapNames

```go
func (re *Regexp) CapNames() []string
```

CapNames walks the regexp to find the names of capturing groups.

#### func Regexp.Equal

```go
func (x *Regexp) Equal(y *Regexp) bool
```

Equal reports whether x and y have identical structure.

#### func Regexp.MaxCap

```go
func (re *Regexp) MaxCap() int
```

MaxCap walks the regexp to find the maximum capture index.

#### func Regexp.Simplify

```go
func (re *Regexp) Simplify() *Regexp
```

Simplify returns a regexp equivalent to re but without counted repetitions and with various other simplifications, such as rewriting /(?:a+)+/ to /a+/. The resulting regexp will execute correctly but its string representation will not produce the same parse tree, because capturing parentheses may have been duplicated or removed. For example, the simplified form for /(x){1,2}/ is /(x)(x)?/ but both parentheses capture as $1. The returned regexp may share structure with or be the original.

#### func Regexp.String

```go
func (re *Regexp) String() string
```

String returns the regular expression in parseable form.
