<!-- Generated by mini-go doc. Target: minigo. -->

# Package `net/url`

```go
package url
```

Package url parses URLs and implements query escaping.

See RFC 3986. This package generally follows RFC 3986, except where it deviates for compatibility reasons. RFC 6874 followed for IPv6 zone literals.

## Index

- [type Error](#type-error)
  - [Error.Error](#func-errorerror)
  - [Error.Temporary](#func-errortemporary)
  - [Error.Timeout](#func-errortimeout)
  - [Error.Unwrap](#func-errorunwrap)
- [type EscapeError](#type-escapeerror)
  - [EscapeError.Error](#func-escapeerrorerror)
- [type InvalidHostError](#type-invalidhosterror)
  - [InvalidHostError.Error](#func-invalidhosterrorerror)
- [func JoinPath](#func-joinpath)
- [func Parse](#func-parse)
- [func ParseQuery](#func-parsequery)
- [func ParseRequestURI](#func-parserequesturi)
- [func PathEscape](#func-pathescape)
- [func PathUnescape](#func-pathunescape)
- [func QueryEscape](#func-queryescape)
- [func QueryUnescape](#func-queryunescape)
- [type URL](#type-url)
  - [URL.AppendBinary](#func-urlappendbinary)
  - [URL.EscapedFragment](#func-urlescapedfragment)
  - [URL.EscapedPath](#func-urlescapedpath)
  - [URL.Hostname](#func-urlhostname)
  - [URL.IsAbs](#func-urlisabs)
  - [URL.JoinPath](#func-urljoinpath)
  - [URL.MarshalBinary](#func-urlmarshalbinary)
  - [URL.Parse](#func-urlparse)
  - [URL.Port](#func-urlport)
  - [URL.Query](#func-urlquery)
  - [URL.Redacted](#func-urlredacted)
  - [URL.RequestURI](#func-urlrequesturi)
  - [URL.ResolveReference](#func-urlresolvereference)
  - [URL.String](#func-urlstring)
  - [URL.UnmarshalBinary](#func-urlunmarshalbinary)
- [func User](#func-user)
- [func UserPassword](#func-userpassword)
- [type Userinfo](#type-userinfo)
  - [Userinfo.Password](#func-userinfopassword)
  - [Userinfo.String](#func-userinfostring)
  - [Userinfo.Username](#func-userinfousername)
- [type Values](#type-values)
  - [Values.Add](#func-valuesadd)
  - [Values.Del](#func-valuesdel)
  - [Values.Encode](#func-valuesencode)
  - [Values.Get](#func-valuesget)
  - [Values.Has](#func-valueshas)
  - [Values.Set](#func-valuesset)

## Functions

### func JoinPath

```go
func JoinPath(base string, elements ...string) (string, error)
```

JoinPath returns a [URL](url.md#type-url) string with the provided path elements joined to the existing path of base and the resulting path cleaned of any ./ or ../ elements. Path elements must already be in escaped form, as produced by [PathEscape](url.md#func-pathescape).

### func Parse

```go
func Parse(rawURL string) (*URL, error)
```

Parse parses a raw url into a [URL](url.md#type-url) structure.

The url may be relative (a path, without a host) or absolute (starting with a scheme). Trying to parse a hostname and path without a scheme is invalid but may not necessarily return an error, due to parsing ambiguities.

### func ParseQuery

```go
func ParseQuery(query string) (Values, error)
```

ParseQuery parses the URL-encoded query string and returns a map listing the values specified for each key. ParseQuery always returns a non-nil map containing all the valid query parameters found; err describes the first decoding error encountered, if any.

Query is expected to be a list of key=value settings separated by ampersands. A setting without an equals sign is interpreted as a key set to an empty value. Settings containing a non-URL-encoded semicolon are considered invalid.

### func ParseRequestURI

```go
func ParseRequestURI(rawURL string) (*URL, error)
```

ParseRequestURI parses a raw url into a [URL](url.md#type-url) structure. It assumes that url was received in an HTTP request, so the url is interpreted only as an absolute URI or an absolute path. The string url is assumed not to have a #fragment suffix. (Web browsers strip #fragment before sending the URL to a web server.)

### func PathEscape

```go
func PathEscape(value string) string
```

PathEscape escapes the string so it can be safely placed inside a [URL](url.md#type-url) path segment, replacing special characters (including /) with %XX sequences as needed.

### func PathUnescape

```go
func PathUnescape(value string) (string, error)
```

PathUnescape does the inverse transformation of [PathEscape](url.md#func-pathescape), converting each 3-byte encoded substring of the form "%AB" into the hex-decoded byte 0xAB. It returns an error if any % is not followed by two hexadecimal digits.

PathUnescape is identical to [QueryUnescape](url.md#func-queryunescape) except that it does not unescape '+' to ' ' (space).

### func QueryEscape

```go
func QueryEscape(value string) string
```

QueryEscape escapes the string so it can be safely placed inside a [URL](url.md#type-url) query.

### func QueryUnescape

```go
func QueryUnescape(value string) (string, error)
```

QueryUnescape does the inverse transformation of [QueryEscape](url.md#func-queryescape), converting each 3-byte encoded substring of the form "%AB" into the hex-decoded byte 0xAB. It returns an error if any % is not followed by two hexadecimal digits.

### func User

```go
func User(username string) *Userinfo
```

User returns a [Userinfo](url.md#type-userinfo) containing the provided username and no password set.

### func UserPassword

```go
func UserPassword(username, password string) *Userinfo
```

UserPassword returns a [Userinfo](url.md#type-userinfo) containing the provided username and password.

This functionality should only be used with legacy web sites. RFC 2396 warns that interpreting Userinfo this way “is NOT RECOMMENDED, because the passing of authentication information in clear text (such as URI) has proven to be a security risk in almost every case where it has been used.”

## Types

### type Error

```go
type Error struct { ... }
```

Error reports an error and the operation and URL that caused it.

#### field Error.Op

```go
Op string
```

Op is the operation that failed.

#### field Error.URL

```go
URL string
```

URL is the URL used by the failed operation.

#### field Error.Err

```go
Err error
```

Err is the underlying error.

#### func Error.Error

```go
func (e *Error) Error() string
```

Error returns a description of the failed URL operation.

#### func Error.Temporary

```go
func (e *Error) Temporary() bool
```

Temporary reports whether the underlying error represents a temporary condition.

#### func Error.Timeout

```go
func (e *Error) Timeout() bool
```

Timeout reports whether the underlying error represents a timeout.

#### func Error.Unwrap

```go
func (e *Error) Unwrap() error
```

Unwrap returns the underlying error.

### type EscapeError

```go
type EscapeError string
```

EscapeError reports an invalid URL escape sequence.

#### func EscapeError.Error

```go
func (e EscapeError) Error() string
```

Error returns a description of the invalid URL escape sequence.

### type InvalidHostError

```go
type InvalidHostError string
```

InvalidHostError reports an invalid character in a URL host name.

#### func InvalidHostError.Error

```go
func (e InvalidHostError) Error() string
```

Error returns a description of the invalid host name.

### type URL

```go
type URL struct { ... }
```

A URL represents a parsed URL (technically, a URI reference).

The general form represented is:

	[scheme:][//[userinfo@]host][/]path[?query][#fragment]

URLs that do not start with a slash after the scheme are interpreted as:

	scheme:opaque[?query][#fragment]

The Host field contains the host and port subcomponents of the URL. When the port is present, it is separated from the host with a colon. When the host is an IPv6 address, it must be enclosed in square brackets: "\[fe80::1]:80". The [net.JoinHostPort](../net.md#joinhostport) function combines a host and port into a string suitable for the Host field, adding square brackets to the host when necessary.

Note that the Path field is stored in decoded form: /%47%6f%2f becomes /Go/. A consequence is that it is impossible to tell which slashes in the Path were slashes in the raw URL and which were %2f. This distinction is rarely important, but when it is, the code should use the [URL.EscapedPath](url.md#func-urlescapedpath) method, which preserves the original encoding of Path. The Fragment field is also stored in decoded form, use [URL.EscapedFragment](url.md#func-urlescapedfragment) to retrieve the original encoding.

The [URL.String](url.md#func-urlstring) method uses the [URL.EscapedPath](url.md#func-urlescapedpath) method to obtain the path.

#### field URL.Scheme

```go
Scheme string
```

Scheme identifies the URL scheme without the trailing colon.

#### field URL.Opaque

```go
Opaque string
```

encoded opaque data

#### field URL.User

```go
User *Userinfo
```

username and password information

#### field URL.Host

```go
Host string
```

"host" or "host:port" (see Hostname and Port methods)

#### field URL.Path

```go
Path string
```

path (relative paths may omit leading slash)

#### field URL.Fragment

```go
Fragment string
```

fragment for references (without '#')

#### field URL.RawQuery

```go
RawQuery string
```

RawQuery contains the encoded query values, without the initial '?'. Use URL.Query to decode the query.

#### field URL.RawPath

```go
RawPath string
```

RawPath is an optional field containing an encoded path hint. See the EscapedPath method for more details.

In general, code should call EscapedPath instead of reading RawPath.

#### field URL.RawFragment

```go
RawFragment string
```

RawFragment is an optional field containing an encoded fragment hint. See the EscapedFragment method for more details.

In general, code should call EscapedFragment instead of reading RawFragment.

#### field URL.ForceQuery

```go
ForceQuery bool
```

ForceQuery indicates whether the original URL contained a query ('?') character. When set, the String method will include a trailing '?', even when RawQuery is empty.

#### field URL.OmitHost

```go
OmitHost bool
```

OmitHost indicates the URL has an empty host (authority). When set, the String method will not include the host when it is empty.

#### func URL.AppendBinary

```go
func (u *URL) AppendBinary(dst []byte) ([]byte, error)
```

AppendBinary appends the binary representation of the URL to dst.

#### func URL.EscapedFragment

```go
func (u *URL) EscapedFragment() string
```

EscapedFragment returns the escaped form of u.Fragment. In general there are multiple possible escaped forms of any fragment. EscapedFragment returns u.RawFragment when it is a valid escaping of u.Fragment. Otherwise EscapedFragment ignores u.RawFragment and computes an escaped form on its own. The [URL.String](url.md#func-urlstring) method uses EscapedFragment to construct its result. In general, code should call EscapedFragment instead of reading u.RawFragment directly.

#### func URL.EscapedPath

```go
func (u *URL) EscapedPath() string
```

EscapedPath returns the escaped form of u.Path. In general there are multiple possible escaped forms of any path. EscapedPath returns u.RawPath when it is a valid escaping of u.Path. Otherwise EscapedPath ignores u.RawPath and computes an escaped form on its own. The [URL.String](url.md#func-urlstring) and [URL.RequestURI](url.md#func-urlrequesturi) methods use EscapedPath to construct their results. In general, code should call EscapedPath instead of reading u.RawPath directly.

#### func URL.Hostname

```go
func (u *URL) Hostname() string
```

Hostname returns u.Host, stripping any valid port number if present.

If the result is enclosed in square brackets, as literal IPv6 addresses are, the square brackets are removed from the result.

#### func URL.IsAbs

```go
func (u *URL) IsAbs() bool
```

IsAbs reports whether the [URL](url.md#type-url) is absolute. Absolute means that it has a non-empty scheme.

#### func URL.JoinPath

```go
func (u *URL) JoinPath(elements ...string) *URL
```

JoinPath returns a new [URL](url.md#type-url) with the provided path elements joined to any existing path and the resulting path cleaned of any ./ or ../ elements. Any sequences of multiple / characters will be reduced to a single /. Path elements must already be in escaped form, as produced by [PathEscape](url.md#func-pathescape).

#### func URL.MarshalBinary

```go
func (u *URL) MarshalBinary() ([]byte, error)
```

MarshalBinary returns the binary representation of the URL.

#### func URL.Parse

```go
func (u *URL) Parse(reference string) (*URL, error)
```

Parse parses a [URL](url.md#type-url) in the context of the receiver. The provided URL may be relative or absolute. Parse returns nil, err on parse failure, otherwise its return value is the same as [URL.ResolveReference](url.md#func-urlresolvereference).

#### func URL.Port

```go
func (u *URL) Port() string
```

Port returns the port part of u.Host, without the leading colon.

If u.Host doesn't contain a valid numeric port, Port returns an empty string.

#### func URL.Query

```go
func (u *URL) Query() Values
```

Query parses RawQuery and returns the corresponding values. It silently discards malformed value pairs. To check errors use [ParseQuery](url.md#func-parsequery).

#### func URL.Redacted

```go
func (u *URL) Redacted() string
```

Redacted is like [URL.String](url.md#func-urlstring) but replaces any password with "xxxxx". Only the password in u.User is redacted.

#### func URL.RequestURI

```go
func (u *URL) RequestURI() string
```

RequestURI returns the encoded path?query or opaque?query string that would be used in an HTTP request for u.

#### func URL.ResolveReference

```go
func (u *URL) ResolveReference(reference *URL) *URL
```

ResolveReference resolves a URI reference to an absolute URI from an absolute base URI u, per RFC 3986 Section 5.2. The URI reference may be relative or absolute. ResolveReference always returns a new [URL](url.md#type-url) instance, even if the returned URL is identical to either the base or reference. If ref is an absolute URL, then ResolveReference ignores base and returns a copy of ref.

#### func URL.String

```go
func (u *URL) String() string
```

String reassembles the [URL](url.md#type-url) into a valid URL string. The general form of the result is one of:

	scheme:opaque?query#fragment
	scheme://userinfo@host/path?query#fragment

If u.Opaque is non-empty, String uses the first form; otherwise it uses the second form. Any non-ASCII characters in host are escaped. To obtain the path, String uses u.EscapedPath().

In the second form, the following rules apply:

  - if u.Scheme is empty, scheme: is omitted.
  - if u.User is nil, userinfo@ is omitted.
  - if u.Host is empty, host/ is omitted.
  - if u.Scheme and u.Host are empty and u.User is nil, the entire scheme://userinfo@host/ is omitted.
  - if u.Host is non-empty and u.Path begins with a /, the form host/path does not add its own /.
  - if u.RawQuery is empty, ?query is omitted.
  - if u.Fragment is empty, #fragment is omitted.

#### func URL.UnmarshalBinary

```go
func (u *URL) UnmarshalBinary(data []byte) error
```

UnmarshalBinary parses a binary URL representation into the receiver.

### type Userinfo

```go
type Userinfo struct { ... }
```

The Userinfo type is an immutable encapsulation of username and password details for a [URL](url.md#type-url). An existing Userinfo value is guaranteed to have a username set (potentially empty, as allowed by RFC 2396), and optionally a password.

#### func Userinfo.Password

```go
func (u *Userinfo) Password() (string, bool)
```

Password returns the password in case it is set, and whether it is set.

#### func Userinfo.String

```go
func (u *Userinfo) String() string
```

String returns the encoded userinfo information in the standard form of "username\[:password]".

#### func Userinfo.Username

```go
func (u *Userinfo) Username() string
```

Username returns the username.

### type Values

```go
type Values map[string][]string
```

Values maps a string key to a list of values. It is typically used for query parameters and form values. Unlike in the http.Header map, the keys in a Values map are case-sensitive.

#### func Values.Add

```go
func (v Values) Add(key, value string)
```

Add adds the value to key. It appends to any existing values associated with key.

#### func Values.Del

```go
func (v Values) Del(key string)
```

Del deletes the values associated with key.

#### func Values.Encode

```go
func (v Values) Encode() string
```

Encode encodes the values into “URL encoded” form ("bar=baz&foo=quux") sorted by key.

#### func Values.Get

```go
func (v Values) Get(key string) string
```

Get gets the first value associated with the given key. If there are no values associated with the key, Get returns the empty string. To access multiple values, use the map directly.

#### func Values.Has

```go
func (v Values) Has(key string) bool
```

Has checks whether a given key is set.

#### func Values.Set

```go
func (v Values) Set(key, value string)
```

Set sets the key to value. It replaces any existing values.
